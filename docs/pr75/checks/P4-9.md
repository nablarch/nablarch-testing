# P4-9 Completion Check — #9 XlsFormatWriter＋ExcelFormatConfig 再構築（Excel OUT）

対象:
- `src/main/java/nablarch/test/tool/converter/xls/XlsFormatWriter.java`
- `src/main/java/nablarch/test/tool/converter/xls/ExcelFormatConfig.java`
- `src/test/java/nablarch/test/tool/converter/xls/XlsFormatWriterTest.java`（33 件 GREEN）

## Completion Criteria

| Criterion | Self-check | Evidence | Review | Review Evidence |
|---|---|---|---|---|
| 全種別を Excel 出力 | OK | TABLE 3 マーカー（SETUP/EXPECTED/COMPLETE）・LIST_MAP・FILE(FIXED/VARIABLE)・MESSAGE・送信系 4 種を build で版面検証。往復は table/list_map/fixed/variable/message/sendSync/複数レコード/null セルの 8 経路を実 `XlsFormatReader` で実証 | OK | QA/SWE 再レビュー PASS |
| 整形はデフォルトで見やすい既定値 | OK | `ExcelFormatConfig.defaults()`＝ヘッダ淡い青／マーカー淡い橙／列幅自動／外枠細線／ブロック間 1 空行。`with*` で上書き可。`appliesHeaderBackgroundColor`/`drawsBlockOuterBorder`/`insertsBlankRowBetweenBlocks`/`appliesAutoColumnWidth`/`honorsConfigOverrides` で実証 | OK | Java/SWE PASS |
| 単体テスト GREEN | OK | `Tests run: 33, Failures: 0, Errors: 0` | OK | — |

カバレッジ（JaCoCo・`mvn package` → `jacoco:report`）:
- `ExcelFormatConfig`: INSTRUCTION/BRANCH/LINE/METHOD **100%**
- `XlsFormatWriter`（内部型含む）: INSTRUCTION 98.9%・BRANCH 96.6%。未到達は **3 番人のみ**＝
  `write` の `parent == null`（basePath は常に親を持つ）／`layout` の sealed 到達不能 `throw`／
  `isMarkerColumn` の `columnName == null`（render は null カラムを扱わない）。**C0/C1 100%（番人除く）**。

## QA Engineer Review（イテレーション 2・PASS）

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| 目的に意味あるテスト | OK | build のセル/スタイル直アサート＋実 Reader 往復の二段構え。版面仕様・記法保存・no 列除去・FW ヘッダ空を検証 |
| エッジケース網羅 | OK | null/空文字/`${}`/マーカー `[...]`/未閉じ括弧/長さ省略 `-`/グループ ID/送信系全 4 種/I/O 失敗 |

イテレーション 1（FAIL）指摘と対応:
1. **(致命) recordType=null × 複数レコードで版面が静かに破壊** → `appendRecords` に番人新設（2 レコード目以降の recordType が null/空なら `IllegalStateException`）。`rejectsNullRecordTypeOnSecondRecord`/`rejectsEmptyRecordTypeOnSecondRecord`（異常系）＋ `allowsNullRecordTypeOnSingleRecord`（1 件目省略は許容）で固定。**解消**
2. **null セルの非可逆往復が未固定** → `roundTripsNullCellAsLiteralNullString` で実 Reader 往復し null→"null"・""→"" を固定。**解消**
3. 境界系（送信系/テーブルの往復差・データ行ゼロ等） → 送信系 4 種・テーブル 3 マーカーは build 全網羅、往復は版面対称性検証ゆえ代表経路で十分と判断。nice-to-have の複数レコード往復は `roundTripsMultipleRecordLayouts` を追加。**妥当と再評価**

## Expert Reviews

### Language Expert（PASS）

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| ベストプラクティス | OK | 不変 config・try-with-resources・`UncheckedIOException` ラップ・入力検証・fail-fast |
| コードベーススタイル統一 | OK | `instanceof` ディスパッチ・冗長ジェネリクス・Javadoc 粒度が `XlsFormatReader`/`YamlFormatWriter` と一致 |
| GWT テスト形式 | OK | 全テスト Given/When/Then |

指摘: 未使用 import `ListMapBlock`/`TableDataBlock` → **削除済み**。

### Software Engineer（PASS）

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| 責務分離 | OK | layout（版面組み立て）/ render（描画）/ BlockLayout / Styles / WidthTracker の境界が明確。build と write 分離でテスト容易化 |
| システム整合性 | OK | `TestDataFormatWriter` 契約充足・`XlsFormatReader`(IN) と版面対称・往復 8 経路 GREEN |
| 保守性 | OK | enum で意味分離・スタイルキャッシュで POI 上限回避・マジックナンバー定数化 |

条件付き指摘（recordType=null × 複数レコード）→ 早期失敗の番人＋テストで固定し **無条件 PASS** へ。

## 回帰

全モジュール `mvn package -Dmaven.javadoc.skip=true`: **Tests 1192／Failures 0／Errors 4**。
4 Errors は既知の Mockito 環境起因 2 クラス（`MockHttpRequestTest`/`MockServletExecutionContextTest`）のみ＝
P3-7 ベースライン（0F/4E）と一致＝**#9 起因の新規失敗ゼロ**。本体（released プロダクションコード）ゼロ差分。

## Overall Verdict

- Self-check: OK
- QA: OK（イテレーション 2）
- Language expert: OK
- Software engineer: OK
- Ready for user review: Yes（Operating mode により自律続行＝ユーザーレビューはスキップ可）
