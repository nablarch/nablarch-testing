# #23 Completion Check

**対象**: 期待値0件テーブルでも DB の実データを読む（新事象・問題1）
**正となる設計文書**: `docs/pr75/docs/ntf-empty-table-assertion.md`
**コミット**: `5acf5d2`（再現テストのみ）→ `b6e049a`（修正）。着手前の順序修正は `eb9556e`
**出典の行番号はすべて `b6e049a` 時点のもの。**

**基準コミットについて** — 作業指示は基準を `6007a17` としていたが、着手時点の `HEAD` および
`origin/convert-testdata-excel-to-text` は `f13563c`（`6007a17` の子。`chore: セッションを中断し State を記録する`）だった。
`git diff --stat 6007a17 f13563c` → `docs/pr75/steering.md` の1ファイルのみ（State 節）で `src/` に差分は無いため、
ゲートの差分比較は指示どおり `6007a17` を起点とした。

---

## Completion Criteria

| Criterion | 判定 | Evidence |
|---|---|---|
| カラム名0件の期待値でも DB が読まれ、DB に行が残っていれば検証が失敗することが自動テストで示されている | OK | `AssertionTest#testAssertTableEqualsWithEmptyColumnNames`（`src/test/java/nablarch/test/AssertionTest.java:47`）。`VariousDbTestHelper.setUpTable` で `TEST_TABLE` に1行入れたうえで、`setColumnNames(new String[0])`（同 `:58`）・行0件の期待値 `TableData` を `Assertion.assertTableEquals("", expected)` に渡し、捕捉した `AssertionError` のメッセージが `" an unexpected record is included in the table of ["` を含むことを表明する（同 `:65`）。この文言は `Assertion.java:310` の `fail(...)` に由来する |
| 修正が `loadData()` 内に閉じており、`getColumnNames()`・準備データ投入・マスタデータ投入・変換ツール経路に変更がない | OK | `git diff --stat 6007a17 HEAD -- src/main/` → `src/main/java/nablarch/test/core/db/TableData.java \| 9 +++++----`（1ファイル・5挿入4削除）。差分は `loadData()`（`TableData.java:337`）の分岐（`:345`）1箇所のみで、局所変数 `colNames` を差し替えるだけ。フィールド `columnNames` は書き換えない（`getClone()`（`:578`）が `Object#clone()` の浅いコピーで配列参照を期待値と共有するため）。`getColumnNames()`（`:502`）の定義に差分なし |
| 現行動作へ戻すための設定が追加されていない | OK | 上記のとおり差分は `loadData()` 内の3行のコメントと1行の代入のみ。設定項目・分岐フラグの追加なし（設計文書 6.3） |
| 表形式リーダ（`TableDataParser`・`ListMapParser`・`TestDataParsingTemplate`）に変更が無い | OK | `git diff 6007a17 HEAD -- src/main/java/nablarch/test/core/reader/` → 出力なし |
| `nablarch-testing-yaml` の `@Ignore` 4件が解除待ちであることが、引き継ぎとして記録されている | OK | 本ファイル末尾「引き継ぎ」節。一次情報は設計文書 付録B.2 |

---

## 実装

`src/main/java/nablarch/test/core/db/TableData.java:337-348`

```java
    public void loadData() {

        String[] colNames = getColumnNames();

        // 取得対象カラムが1つも存在しない場合は、DBの全カラムを取得対象とする。
        // カラム名の宣言は「検証対象カラムの絞り込み」であって「検証するかどうか」の
        // スイッチではないため、カラム名が0件でもDBは読む。
        // （docs/pr75/docs/ntf-empty-table-assertion.md 4.1）
        if (colNames.length == 0) {
            colNames = dbInfo.getColumns(tableName);
        }
```

作業指示 §2-1 が示した形（局所変数の差し替え・`setColumnNames` を呼ばない）どおりである。

---

## ゲート実測（2026-08-21）

実行コマンドはいずれも `JAVA_HOME=/usr/lib/jvm/temurin-17-jdk-amd64 LANG=ja_JP.UTF-8 TZ=Asia/Tokyo mvn -o clean test`。

| # | ゲート | 結果 | 実測値 |
|---|---|---|---|
| 1 | 全件テストが `Failures: 0, Errors: 0` で `BUILD SUCCESS`。件数は `851` ＋ 追加分 | OK | `Tests run: 852, Failures: 0, Errors: 0, Skipped: 7` / `BUILD SUCCESS`。`852 = 851 + 1`（追加した再現テスト1件） |
| 2 | Skipped の内訳が基準から不変（計7） | OK | `HtmlConvertTest` 2・`RequestTestingMessagingClientTest` 2・`DbAccessTestSupportTest` 2・`FileUtilsTest` 1 = 7 |
| 3 | RED の実測。失敗が「`assertTableEquals` が落ちなかった」ことによるものであること | OK | 修正前 `mvn -o test -Dtest=AssertionTest` → `Tests run: 46, Failures: 1`。<br>`AssertionTest.testAssertTableEqualsWithEmptyColumnNames:64`<br>`Expected: a string containing " an unexpected record is included in the table of ["`<br>`     but: was "期待したエラーが発生しませんでした。"`<br>捕捉した `AssertionError` のメッセージが `fail("期待したエラーが発生しませんでした。")` の文言そのものであり、**`assertTableEquals` が落ちなかった**ことを示す |
| 4 | GREEN の実測。捕まえた `AssertionError` のメッセージが期待どおりであること | OK | 修正後 `Tests run: 46, Failures: 0, Errors: 0, Skipped: 0 - in nablarch.test.AssertionTest`。メッセージ全文（一時計測クラスで採取）:<br>`java.lang.AssertionError:  an unexpected record is included in the table of [TEST_TABLE]. PK=[PK_COL1=00001,PK_COL2=1]` |
| 5 | 負のコントロール。分岐を元の early return に戻すと再現テストだけが落ちる | OK | 変異状態で全件実行 → `Tests run: 856, Failures: 1, Errors: 0, Skipped: 7` / `BUILD FAILURE`。失敗は `AssertionTest.testAssertTableEqualsWithEmptyColumnNames:64` の1件のみ。<br>（`856 = 852 + 4`。付録C 計測用の一時クラス `Tmp23MeasurementTest` 4件を同時に走らせていたため。計測後に削除済み）<br>復元後 `git status --porcelain` → 出力なし |
| 6 | `src/main` の差分が `TableData.java` の1ファイルのみ | OK | `git diff --stat 6007a17 HEAD -- src/main/` → `src/main/java/nablarch/test/core/db/TableData.java \| 9 +++++----` / `1 file changed, 5 insertions(+), 4 deletions(-)` |
| 7 | 公開 API（`public`／`protected`）の宣言変更が0件 | OK | `git diff 6007a17 HEAD -- src/main/ \| grep -E "^[+-] *(public\|protected)"` → 出力なし |
| 8 | `getColumnNames()`・表形式リーダ3クラスに差分が無い | OK | `git diff 6007a17 HEAD -- src/main/java/nablarch/test/core/reader/` → 出力なし。`git diff 6007a17 HEAD -- src/main/ \| grep getColumnNames` の該当は文脈行 `         String[] colNames = getColumnNames();` の1行のみで、`+`/`-` 行に `getColumnNames` は現れない |
| 9 | `steering.md` と `docs/pr75/checks/23.md` を更新。過去の記録は削除しない | OK | `steering.md` は Prerequisites 2箇所と State を更新（`eb9556e`）、`#23` の Steps を全件チェック。`checks/21.md` を含む過去の記録に削除なし |
| 10 | `checks/23.md` に書いた識別子と行番号が現行コードに実在すること | OK | 次節 |

### ゲート10 — 本ファイルに書いた出典の実在確認

```
$ grep -n "public void loadData\|colNames.length == 0\|public String\[\] getColumnNames\|public String\[\] getPrimaryKeys\|public TableData getClone" src/main/java/nablarch/test/core/db/TableData.java
337:    public void loadData() {
345:        if (colNames.length == 0) {
481:    public String[] getPrimaryKeys() {
502:    public String[] getColumnNames() {
578:    public TableData getClone() {

$ grep -n "public static void assertTableEquals(String message, TableData expected)\|String\[\] columns = expected.getColumnNames();\|an unexpected record is included" src/main/java/nablarch/test/Assertion.java
79:    public static void assertTableEquals(String message, TableData expected) {
256:        String[] columns = expected.getColumnNames();
310:                fail(message, " an unexpected record is included in the table of [",  actual.getTableName(), "].",

$ grep -n "testAssertTableEqualsWithEmptyColumnNames\|setColumnNames(new String\[0\])\|containsString(\" an unexpected" src/test/java/nablarch/test/AssertionTest.java
47:    public void testAssertTableEqualsWithEmptyColumnNames() {
58:        expected.setColumnNames(new String[0]);
65:                    containsString(" an unexpected record is included in the table of ["));

$ grep -n "getEffectiveColumnNames\|MARKER_COLUMN_CONDITION" src/main/java/nablarch/test/core/reader/HeaderLine.java
40:        markerIndices.addAll(wrapper.indicesOf(MARKER_COLUMN_CONDITION));
41:        effectiveColumnNames = wrapper.exclude(MARKER_COLUMN_CONDITION);
49:    String[] getEffectiveColumnNames() {
88:    private static final ListWrapper.Condition<String> MARKER_COLUMN_CONDITION

$ grep -n "getEffectiveColumnNames\|new TableData(dbInfo" src/main/java/nablarch/test/core/reader/TableDataParser.java
112:        String[] columnNames = header.getEffectiveColumnNames();
114:        processing = new TableData(dbInfo, tableName, columnNames, defaultValues);

$ grep -n "getColumns" src/main/java/nablarch/test/core/db/DbInfo.java
29:    String[] getColumns(String tabName);
```

**0件のものは無い。**

---

## 付録C の未確認事項 — 実測（2026-08-21）

計測は一時クラス `src/test/java/nablarch/test/core/reader/Tmp23MeasurementTest.java` で行い、計測後に削除した。
リポジトリには残していない。再実行が必要な場合は下記の条件で書き直すこと。

- `@RunWith(DatabaseTestRunner.class)`／`SystemRepositoryResource("unit-test.xml")`／`TEST_TABLE` に1行投入
- 計測2・3 は `TableDataParser` にインメモリ `TestDataReader` で行リストを直接流す（`parse(dir, res, "", false)` でキャッシュを回避）。**POI のセル読み取り（`XlsReader`）は通していない**

### 1. カラム名0件かつ行が1件以上ある期待値

期待値: `setColumnNames(new String[0])` ＋ 空の行を1件 `addRow`。DB には1行ある。

| | 結果 |
|---|---|
| 修正前 | `AssertionError:  the table of [TEST_TABLE] is expected to have a record whose PK is [PK_COL1=null,PK_COL2=null], but there is no such record in the table. row number=[1]` |
| 修正後 | **同一のメッセージ**（上と同じ） |

**本変更でこの形の挙動は変わらなかった。** 期待値の行が PK 値を持たない（`PK_COL1=null,PK_COL2=null`）ため、
修正前は「実データ0行」、修正後は「実データ1行だが PK が一致しない」で、いずれも `Assertion.java` の
PK 不一致 `fail`（`Assertion.java:288-293`。メッセージ本体は `:290`）に到達する。付録C が「別の失敗の仕方になる可能性がある」と
していた点は、**本実測の範囲では別の失敗にはならなかった**。

### 2. ヘッダ行がマーカーカラムのみの `EXPECTED_TABLE`

入力（`TableDataParser`／`DataType.EXPECTED_TABLE_DATA`）:

```
EXPECTED_TABLE=test_table
[空]
```

パース結果は修正前後とも `blocks=1` / `table=TEST_TABLE columnNames=[] rows=0`。
その `TableData` を `Assertion.assertTableEquals` に渡した結果:

| | 結果 |
|---|---|
| 修正前 | `no error`（検証が素通りする） |
| 修正後 | `AssertionError:  an unexpected record is included in the table of [TEST_TABLE]. PK=[PK_COL1=00001,PK_COL2=1]` |

**この形が誤りか意図どおりかは記法に定義が無く判断できない（設計文書 3.2）。ここでは挙動のみを記録し、判断しない。**

### 3. 対照 — マーカーカラムのみのヘッダ行でも次のブロックが無傷であること（設計文書 付録B.1 の再確認）

入力:

```
EXPECTED_TABLE=EMPTY_T
[空]
EXPECTED_TABLE=test_table
PK_COL1  PK_COL2
00001    1
```

修正前後とも結果は同じ:

```
table=EMPTY_T    columnNames=[]                 rows=0
table=TEST_TABLE columnNames=[PK_COL1, PK_COL2] rows=1
```

設計文書 付録B.1 の実測（2026-08-19）と一致する。

---

## 追加作業（2026-08-21）— 「空なら通る」側の担保

**背景。** `#23` の目的は「DB が空なら通り、行が残っていれば落ちる」の両立である。`b6e049a` 時点で
担保されていたのは**落ちる側だけ**だった。長さ0のカラム名を使うコミット済みのテストは再現テスト
`testAssertTableEqualsWithEmptyColumnNames` の1件のみで（`grep -rn "new String\[0\]" src/test`）、
「カラム名0件を一律で失敗として扱う」変更を入れても既存の852件は1件も落ちなかった（下記ゲート3で実測）。

**追加したもの。** `src/test/java/nablarch/test/AssertionTest.java:76-85`
`AssertionTest#testAssertTableEqualsWithEmptyColumnNamesOnEmptyTable`（再現テストの直後）。
`TEST_TABLE` は `AssertionTestSupport.java:68` の `@Before`（`VariousDbTestHelper.delete(TestTable.class)`）で
各テストの実行前に空になるため行を入れず、`setColumnNames(new String[0])` を明示した期待値を
`Assertion.assertTableEquals("", expected)` に渡して例外が投げられないことを表明する。
実装（`TableData#loadData()`）は1行も変えていない。

### ゲート実測

| # | ゲート | 結果 | 実測値 |
|---|---|---|---|
| 1 | 全件テスト（`852` ＋ 追加分） | OK | `Tests run: 853, Failures: 0, Errors: 0, Skipped: 7` / `BUILD SUCCESS`。`853 = 852 + 1` |
| 2 | Skipped の内訳が基準から不変（計7） | OK | `HtmlConvertTest` 2・`RequestTestingMessagingClientTest` 2・`DbAccessTestSupportTest` 2・`FileUtilsTest` 1 = 7 |
| 3 | ユニークキル変異（下記）で**追加テストだけが落ち、再現テストは生き残る** | OK | `mvn -o test -Dtest=AssertionTest` → `Tests run: 47, Failures: 1, Errors: 0, Skipped: 0`。失敗は `AssertionTest.testAssertTableEqualsWithEmptyColumnNamesOnEmptyTable:84`（メッセージ ` an unexpected record is included in the table of [TEST_TABLE].`）の1件のみ |
| 4 | 変異の復元 | OK | 原本を書き戻し `git diff -- src/main/java/nablarch/test/Assertion.java` → 出力なし |
| 5 | `src/main` の差分が無いこと | OK | `git diff 3b87338 -- src/main/` → 出力なし（0行） |

### ゲート3 の変異 — 「カラム名0件を一律で失敗にする」将来の後退の模擬

`src/main/java/nablarch/test/Assertion.java:79` の `assertTableEquals(String, TableData)` 冒頭に挿入した。

```java
    public static void assertTableEquals(String message, TableData expected) {
        if (expected.getColumnNames().length == 0) {
            fail(message, " an unexpected record is included in the table of [", expected.getTableName(), "].");
        }
        TableData actual = expected.getClone();
```

再現テストは投げられた `AssertionError` のメッセージがまさにこの文言を含むことを表明しているため**生き残る**。
追加テストだけが落ちる。**この非対称性が、追加テストが既存のどのテストでも代替できない理由そのものである。**

### 付録C の計測1・計測2 の扱い — 残さないと決めた（2026-08-21）

回帰テストとして**残さない**。計測2（マーカーカラムだけのヘッダ行を持つ `EXPECTED_TABLE` が検証されるようになる形）は
**記法側に定義が無く**（設計文書 `ntf-empty-table-assertion.md` 3.2）、定義の無い形をテストで固定すると
仕様として確定してしまうため。計測1 は修正前後で挙動が変わらず（本ファイル 付録C-1）、守るべき差分が無い。

---

## 後方互換

**本リポジトリでは該当0件（2026-08-19 実測。59ファイル・242シート・テーブル系識別子行324箇所。設計文書 5.2）。
利用PJのテストデータは未走査（同 5.5）。** 全件テスト `Tests run: 852, Failures: 0` は本リポジトリ内の裏付けであり、
利用PJへの影響の有無を示すものではない。

---

## 引き継ぎ

- **`nablarch-testing-yaml` の `@Ignore` 4件**（設計文書 付録B.2）は、本変更を `install` した後に解除する。あわせて `YamlTableDataBuilder.java:110-114` の FIXME を削除する
  - `YamlTestDataParserTest#emptyExpectedTable_failsWhenDbHasRows`
  - `YamlTableDataBuilderTest#buildTableDataList_emptyRowsExcluded`
  - `YamlTableDataBuilderTest#buildTableDataList_allEmptyRowsReturnsTableDataWithAllDbColumns`
  - `YamlTableDataBuilderTest#buildTableDataList_emptyExpectedTableReturnsTableDataWithAllDbColumns`
- **`nablarch-testing-converter`**（設計文書 付録B.1）と **`nablarch-document`**（同 B.3）への申し送りは設計文書のとおり。本リポジトリでの対応は無い

---

## ユーザー判断を仰ぐ事項

1. ~~**付録C の計測1・2 を回帰テストとして残すか。**~~ → **残さないと決めた（2026-08-21）。** 理由は上節「付録C の計測1・計測2 の扱い」
2. ~~**レビュー（QA・Craft・Verification）を回すか、回すなら誰を回すか**~~ → **回さないと決めた（2026-08-21）。** 代わりに「DBが空なら通る」側を守るテストを1件追加することで決着し、そのテスト（`AssertionTest#testAssertTableEqualsWithEmptyColumnNamesOnEmptyTable`）は `36cabc6` で入っている。詳細は上節「追加作業（2026-08-21）— 「空なら通る」側の担保」
