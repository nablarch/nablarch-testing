# P2-3 (#3) Completion Check — 変換ツール一括削除＋テスト基盤一時無効化

タスク: Phase 2 #3 / ブランチ `add-yaml` / 削除コミット直前の基点 `5a160c4`

## 1. 削除した変換ツール（`tool/converter/`）

- `src/main/java/nablarch/test/tool/converter/` 配下 **24 ファイル**を `git rm`（main 一括削除）
- `src/test/java/nablarch/test/tool/converter/` 配下 **9 ファイル**を `git rm`（test 一括削除。すべて `.java`。変換ツールにテストリソース/データは存在しないため Java のみ）
- 削除合計 **33 ファイル**（main 24 ＋ test 9）
  - test 側 Java テストクラス 9 本: `ConverterFileFilterTest` / `ConverterPathResolverTest` / `DataFormatTest` / `TestDataConverterTest` / `xls/XlsFormatReaderTest` / `xls/XlsFormatWriterTest` / `yaml/YamlFormatReaderTest` / `yaml/YamlFormatWriterTest` / `yaml/YamlTestDataValidatorTest`
- 削除後 `tool/` 配下に残るのは `htmlcheck` / `sanitizingcheck`（変換ツールと無関係・無変更）のみ

**重要（同名異物の非対象）**: `nablarch.test.core.file.TestDataConverter`（FixedLengthFile の電文変換用 released インタフェース）と、そのコンポーネント設定 `TestDataConverter_Hoge` / `TestDataConverter_UTF8_ENCODE`（messaging/file 系テスト・component XML）は**別物で本削除の対象外**。これらは温存。削除対象は `nablarch.test.tool.converter.*` のみ。

## 2. 一時無効化したテスト基盤（#13 で復旧）

### `YamlModeTestBase`（`src/test/java/nablarch/test/core/reader/yaml/`）
- これはテストクラスではなくユーティリティのため `@Ignore` では無効化できない（変換ツール削除で**コンパイル不能**になる）。
- 対応: `tool.converter.*` の import と `prepareYamlData` 本体（`ConversionRequest`/`DataFormat`/`TestDataConverter.convert` 呼び出し＋出力複製）を撤去し、`prepareYamlData` を `UnsupportedOperationException` を送出するスタブ化。未使用化した IO import・private ヘルパ（`deleteDirectory`/`copyDirectory`）も撤去。
- `wrapForYaml`・定数 `YAML_ROOT`・private コンストラクタは変換ツール非依存のため温存。
- **復旧方針（#13）**: 基点 `5a160c4` の `prepareYamlData`（Excel→一時 YAML 変換＋自クラス名ディレクトリ複製）を、再構築後の `TestDataConverter.convert(ConversionRequest)` 入口 API（#10 成果物）へ繋ぎ直す。削除した IO ヘルパも同コミットから復元。

### `@Ignore` を付与した 18 個の `*YamlTest`（理由: `Phase 2 (#3): 変換ツール再構築中につき一時無効化。#13 で再有効化する。`）
すべて対応する Excel 版基底テストを継承し、`@BeforeClass` で `YamlModeTestBase.prepareYamlData(...)` を呼ぶ等価性確認テスト。クラスレベル `@Ignore` により `@BeforeClass` を含め全体スキップ。

| # | クラス | パッケージ |
|---|---|---|
| 1 | `TestSupportYamlTest` | `nablarch.test` |
| 2 | `BatchRequestTestSupportYamlTest` | `nablarch.test.core.batch` |
| 3 | `DBtoDBBatchSampleYamlTest` | `nablarch.test.core.batch` |
| 4 | `FileToFileBatchSampleYamlTest` | `nablarch.test.core.batch` |
| 5 | `SimpleBatchSampleYamlTest` | `nablarch.test.core.batch` |
| 6 | `DbAccessTestSupportYamlTest` | `nablarch.test.core.db` |
| 7 | `EntityTestSupportYamlTest` | `nablarch.test.core.db` |
| 8 | `TestBeanYamlTest` | `nablarch.test.core.entity` |
| 9 | `TestEntityYamlTest` | `nablarch.test.core.entity` |
| 10 | `FileSupportYamlTest` | `nablarch.test.core.file` |
| 11 | `FileSupportWithDbLessTestDataParserYamlTest` | `nablarch.test.core.file` |
| 12 | `AbstractHttpRequestTestTemplateYamlTest` | `nablarch.test.core.http` |
| 13 | `MessagingReceiveTestSupportYamlTest` | `nablarch.test.core.messaging` |
| 14 | `MessagingRequestTestSupportYamlTest` | `nablarch.test.core.messaging` |
| 15 | `RequestTestingMessagingClientYamlTest` | `nablarch.test.core.messaging` |
| 16 | `RequestTestingMessagingContextYamlTest` | `nablarch.test.core.messaging` |
| 17 | `RequestTestingSendSyncBatchYamlTest` | `nablarch.test.core.messaging` |
| 18 | `RequestTestingSendSyncSupportYamlTest` | `nablarch.test.core.messaging` |

**復旧方針（#13）**: `YamlModeTestBase.prepareYamlData` の再接続後、18 本の `@Ignore` と `import org.junit.Ignore;` を撤去して再有効化し、#14 で Excel ベースラインと同一結果（全件 PASS）を確認する。

## 3. 残置した変換ツール周辺（#10 で復旧・本タスク対象外）

- `pom.xml:216-224` の `exec-maven-plugin`（`<mainClass>nablarch.test.tool.converter.TestDataConverter</mainClass>`）は削除クラスを指す**残存参照**。本ブロックは PR75 でこの変換ツール CLI 用に追加（commit `20e13c2`・main には無い）。`<executions>` 未設定で手動 `mvn exec:java` 時のみ起動するためコンパイル/テストには影響しない。変換ツール入口・CLI/Mojo の再構築は **#10 のスコープ**ゆえ #10 で再構成する。pom.xml には別途コミット禁止のローカル変更（parent `6u3`）があるため本タスクでは pom を変更しない。

## Completion Criteria（セルフチェック）

| 完了条件 | 判定 | 根拠 |
|---|---|---|
| `tool/converter/` が main/test とも存在しない | OK | `ls src/{main,test}/java/nablarch/test/tool/converter` → No such file。`git rm` で main 24 ＋ test 9（合計 33）ファイル削除（変換ツールにテストリソースなし）。repo 全体 grep で `nablarch.test.tool.converter` の残存参照は `pom.xml` の exec mainClass（#10 対象・§3）と `YamlModeTestBase` の Javadoc/例外文言のみ |
| プロジェクトがコンパイル成功 | OK | `mvn -o test-compile` EXIT=0、エラー出力なし |
| Excel 経路の既存テストが従来 GREEN | OK | `mvn -o test` 全 surefire 集計 **Failures=0**。Error は既存 Mockito 環境起因 2 クラス（`MockHttpRequestTest` E=2 / `MockServletExecutionContextTest` E=2）のみで #3 非起因。ベースライン 16 失敗クラスから「削除した `ConverterFileFilterTest`＋無効化した 13 `*YamlTest`」が消え、想定どおり 2 クラスへ収束 |
| 無効化した 18 テストの一覧と復帰方針が記録済み | OK | 本ファイル §2 に一覧（18 件）・`YamlModeTestBase` スタブ化方針・復旧基点 `5a160c4`・#13/#14 での再有効化手順を記載 |

- セルフチェック総合: **OK**
- カバレッジ測定: **N/A**（本タスクは削除＋無効化のみ。新規プロダクションコード追加なし。`YamlModeTestBase` スタブは番人扱い）

## アーキテクト／SWE レビュー（Phase 2 ゲート）

サブエージェント（独立・会話履歴なし）で 2 観点を実施。

### アーキテクト — **PASS**（イテレーション 1）
- 4 Completion criteria すべて OK。削除は変換ツールに限定、同名異物 `core.file.TestDataConverter` とそのコンポーネント設定（`TestDataConverter_Hoge` 等）を巻き込んでいないことを実物確認。
- 温存した `wrapForYaml`/`YAML_ROOT`/残存 5 import がすべて使用中（未使用 import なし）を確認。
- 復旧基点 `5a160c4` の実在と `prepareYamlData` 完全版の存在を `git show` で確認 → #13 復旧経路が具体的・追跡可能。
- 唯一の残存参照 `pom.xml` exec mainClass は非ビルド経路かつ #10 申し送り済みで適切。

### ソフトウェアエンジニア — **PASS**（イテレーション 2）
- イテレーション 1 で 2 件 NG:
  - **NG-1**: P2-3.md の削除数記述が事実と不一致（「test 33・リソース含む」→ 実際は main 24・test 9・全 .java・リソース 0）。→ §1・Completion 表を訂正し解消。
  - **NG-2**: `import org.junit.Ignore;` が import ブロック先頭に孤立し `org.junit.*` 群を分断。→ 18 ファイルとも `org.junit.*` 群内へ移動し解消。
- イテレーション 2 で両 NG の解消を実物確認。責務分離（ユーティリティ=スタブ化／テスト=@Ignore）・システム整合性（public シグネチャ維持・本体 released 非巻き込み）・保守性（デッドコードなし・復旧情報十分）すべて OK。@Ignore がクラス全体（@BeforeClass/@ClassRule 含む）をスキップすることを実機検証で実証。

**ゲート判定: 通過**（アーキ/SWE 全 PASS）。Operating mode（ユーザー指示）によりユーザーレビューはスキップ。
