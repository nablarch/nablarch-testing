# T7 完了条件チェック

タスク: 等価性テストの拡充（型行を持つ実Excel・messaging 系の並走。旧 V-1 を統合）
参照文書: 仕様リスト SS-05/MS-04、解説書 §6・§7
変更対象: `src/test/.../ExcelToYamlEquivalenceTest.java`（または新規等価性テスト）、テスト用 Excel/YAML

---

## STEP5 完了条件チェック（NtfTestdataTestRunner 新規作成）

**対象コミット**: `ce9e360`（実装）/ `543c00e`（レビュー対応）

| 完了条件 | 担当者判定 | 担当者根拠 |
|---|---|---|
| Runner が `DatabaseTestRunner` を継承している | OK | `NtfTestdataTestRunner extends DatabaseTestRunner` |
| サンプルテストクラスで各メソッドが Excel/YAML の2回実行され両方パスする | OK | `NtfTestdataTestRunnerTest.runnerExecutesEachMethodTwice_excelThenYaml`: `startedCount == 2` かつ失敗ゼロを確認 |
| `mvn test -Dtest=NtfTestdataTestRunnerTest` でも2回実行される | OK | 上記テストが Maven でグリーン（`Tests run: 5, Failures: 0`） |
| 2回目の後 `testDataParser`・resource-root が復元され他テストに影響しない | OK | `afterYamlRun_restoreSucceeds_confirmedByNoFailure`（復元失敗は UNREACHABLE AssertionError として検出）、`afterFailingYamlRun_parserIsStillRestored`（例外時も finally が動作）の2テストで確認 |
| 生成 YAML は `target/` 配下のみ、`src/test` に生成物がない | OK | `GENERATED_YAML_ROOT = "target/generated-test-yaml"`。`find src/test -path "*/generated-test-yaml*"` で空を確認済み |
| `mvn test` 全クラスで STEP4 完了時のベースラインから新規失敗がない | OK | ベースライン `Tests run: 1243, Failures: 1, Errors: 26`。STEP5 後: `Tests run: 1248, Failures: 1, Errors: 26`（+5テストのみ） |

### QAエンジニアレビュー（サブエージェント実施）

| 観点 | 判定 | 根拠・改善案 |
|---|---|---|
| 目的に対して意味のあるテスト・動作確認が実施されているか | OK | 2回実行・YAML差し替え・復元・例外時復元の4観点を確認 |
| エッジケースが漏れなくテスト・動作確認されているか | OK | convert 戻り値チェック追加、FailingTarget による例外時復元確認、@BeforeClass による static リセット対応済み |

QA指摘（NG→対応済み）:
- 変換エラー戻り値を無視していた → `exitCode != 0` で `AssertionError` をスロー
- テスト失敗時の復元確認テストがなかった → `FailingTarget` + テスト追加
- 復元確認テストが定数比較で意味をなしていなかった → テストを再設計

### 対象言語エキスパートレビュー（サブエージェント実施）

| 観点 | 判定 | 根拠・改善案 |
|---|---|---|
| ベストプラクティス準拠 | OK | final ローカル変数コピーの削除、Javadoc 追記 |
| 既存コードスタイル統一 | OK | 既存テストの Javadoc/アクセス修飾子スタイルに準拠 |
| テストコードのGWT形式 | OK | 全テストに Given/When/Then コメントを統一 |

Java エキスパート指摘（NG→対応済み）:
- `loadComponent` 内の不要な final ローカル変数コピー → 削除
- テストの GWT コメント不統一 → 全件統一
- 未使用インポート → 削除

### ソフトウエアエンジニアレビュー（サブエージェント実施）

| 観点 | 判定 | 根拠・改善案 |
|---|---|---|
| 責務分離の適切さ | OK | Runner・YamlSetupRule・IgnoreDetectingNotifier の3クラスで責務を分離 |
| システム全体の整合性 | OK | `@TargetDb` Ignored の二重集計問題を `IgnoreDetectingNotifier` で解消 |
| 保守性・拡張性 | OK | 変換のメソッド単位実行は冪等性を維持し、最適化の必要性はコメントで記録 |

SWE指摘（NG→対応済み）:
- `@TargetDb` Ignored メソッドが二重集計される → `IgnoreDetectingNotifier` + `ignoredMethod_isNotRunTwice` テスト追加
- 変換がメソッド単位で毎回走るパフォーマンス問題 → 現状許容（冪等・STEP6 適用後に必要なら最適化）の方針をコメントに記録

### 総合判定

- 担当者: OK
- QA: OK（2ラウンド後）
- 対象言語エキスパート: OK
- ソフトウエアエンジニア: OK（2ラウンド後）
- ユーザーレビュー可否: 可

---

## STEP6 完了条件チェック（18クラスへの Runner 適用・YAML 不具合全修正）

**ユーザーレビュー**: OK（2026-06-05）

**対象コミット**: `5118fc4`

| 完了条件 | 担当者判定 | 担当者根拠 |
|---|---|---|
| 18クラスに `@RunWith(NtfTestdataTestRunner.class)` が付与されている | OK | STEP5 の `fe43dd6` で付与済み。HttpRequestTestSupportTest は TestDataParser 非使用のため対象外（`@RunWith` 元に戻し） |
| YAML モード (Run 2) で全クラスがグリーン | OK | `mvn test` で `Tests run: 1472, Failures: 0, Errors: 0, Skipped: 7` を確認 |
| Run1→Run2 の static キャッシュ汚染が解消されている | OK | `NtfTestdataTestRunner.clearAllStaticCaches()` を Run2 開始前に呼び出し。PoiXlsReader・TestDataParsingTemplate・ListMapParser・TableDataParser・DataFileParser・YamlLoader・SendSyncSupport・RequestTestingSendSyncSupport・RequestTestingMessagingClient・RequestTestingMessagingContext の全キャッシュをクリア |
| `YamlMessageBuilder.buildSendSyncMessageList` が `[case1]` 形式で正しくマッチする | OK | エントリの `group_id` に `"["` と `"]"` を付与して `groupId` と比較するよう修正。`RequestTestingMessagingClientTest` が 20エラー → 0 に |
| `rows: []` 空テーブルが Excel と同等に DELETE 対象として扱われる | OK | `YamlTableDataBuilder.buildTableDataList` の `rows.isEmpty()` continue を削除し、空 `TableData` を返すよう修正。`MessagingReceiveTestSupportTest` の PK 違反が解消 |
| STEP5 以前に確認したベースラインから新規失敗が増えていない | OK | STEP5 後のベースライン `Tests run: 1248, Failures: 1, Errors: 26` → STEP6 後: `Tests run: 1472, Failures: 0, Errors: 0`（224件増加・失敗・エラーともゼロ） |

---

## 実施手順

1. 型行を持つ実 Excel（`FileSupportTest.xls`、`VariableLengthFileParserTest.xls` 等）を変換ツールで YAML 化し、`BasicTestDataParser`（Excel）と `YamlTestDataParser`（YAML）の読込結果が等価であることを検証する並走テストを追加する。
2. messaging 系の実 Excel（FW制御ヘッダ・電文ボディ・複数データ行を含む。`MessagingRequestTestSupportTest.xls`、`MessageParserTest.xls` 等）を変換し、`directives`/`fwHeader`/電文ボディが Excel と YAML で等価であることを検証する並走テストを追加する。
3. 乱数インタープリタ等で等価比較不可のケースは対象外とし、その理由をテストの Javadoc に明記する（決定的な値で代替確認する）。

## 完了条件チェックリスト

| 完了条件 | 担当者判定 | 担当者根拠 | QA判定 | QA根拠 |
|---|---|---|---|---|
| 型行を持つファイルデータの Excel↔YAML 等価性テストが追加されグリーン | OK / NG | | OK / NG | |
| messaging 系（FW制御ヘッダ・日本語型・複数データ行）の Excel↔YAML 等価性テストが追加されグリーン | OK / NG | | OK / NG | |
| 対象外としたケースの理由が記録されている | OK / NG | | OK / NG | |
| 全テスト（Excel/YAML 両方）が同一結果でグリーン | OK / NG | | OK / NG | |

## QAエンジニアレビュー

| 観点 | 判定 | 根拠・改善案 |
|---|---|---|
| 目的に対して意味のあるテスト・動作確認が実施されているか | OK / NG | |
| エッジケースが漏れなくテスト・動作確認されているか | OK / NG | |

## エキスパートレビュー（ソースコード変更タスクのみ）

### 対象言語エキスパートレビュー

| 観点 | 判定 | 根拠・改善案 |
|---|---|---|
| ベストプラクティス準拠 | OK / NG | |
| 既存コードスタイル統一 | OK / NG | |
| テストコードのGWT形式 | OK / NG | |

### ソフトウエアエンジニアレビュー

| 観点 | 判定 | 根拠・改善案 |
|---|---|---|
| 責務分離の適切さ | OK / NG | |
| システム全体の整合性 | OK / NG | |
| 保守性・拡張性 | OK / NG | |

## 総合判定

- 担当者: OK / NG
- QA: OK / NG
- 対象言語エキスパート: OK / NG / 該当なし
- ソフトウエアエンジニア: OK / NG / 該当なし
- ユーザーレビュー可否: 可 / 不可（理由）
