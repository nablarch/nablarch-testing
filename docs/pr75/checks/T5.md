# T5 完了条件チェック

タスク: 変換ツールに検証モード（リンタ）を追加
参照文書: 設計書 §10（本タスクで追記）
変更対象: 新規 `YamlTestDataValidator`・`ValidationError`（`yaml` パッケージ）、`TestDataConverter` への組込み、対応するテスト

## 実施手順（TDD）

1. RED: 不正 YAML でエラーが報告されることを検証するテストを追加し、空実装で失敗を確認した。
   - 列数不一致（`fields` 件数 ≠ いずれかの `rows` 配列長）
   - 構造境界違反（既知ディレクティブ名が `fw_header:` にある）
   - スキーマ非適合
2. GREEN: 上記3点の検証を実装した。列数一致は各 `record_fragment` で `fields` 件数と各 `rows` 配列長を照合。スキーマ適合は `ntf-testdata-yaml-schema.json` で検証。
3. 失敗時に「ファイル・位置・内容」を構造エラー一覧として出力する。「検証のみ」（`--validate`）と「変換時に検証し失敗で中断」（`--validate-on-convert`）の2モードを提供した。

## 完了条件チェックリスト

| 完了条件 | 担当者判定 | 担当者根拠 | QA判定 | QA根拠 |
|---|---|---|---|---|
| 列数不一致を検出する | OK | `YamlTestDataValidatorTest#columnCountMismatch_setupFiles/messages/secondRowOnly` でアサート済み | OK / NG | |
| ディレクティブ/FW制御ヘッダの取り違えを検出する | OK | `directiveInFwHeader_knownDirectiveName/multipleKeys` でアサート済み | OK / NG | |
| スキーマ非適合を検出する | OK | `schemaViolation_tableIsArray` でアサート済み | OK / NG | |
| 正常な YAML は検証を通過する | OK | `allRulesPassed_noError`・`columnCountMatch_noError`・`fwHeader_nonDirectiveKey_noError`・`schemaCompliant_noError` でアサート済み | OK / NG | |
| 検証のみモードと変換時検証モードの両方が動作する | OK | `TestDataConverterTest#validateModeWithValidYaml_returnsCode0/Invalid`・`validateOnConvertWithValidYaml/Invalid` でアサート済み | OK / NG | |

## カバレッジ

| クラス | 行カバレッジ | 分岐カバレッジ | 未達理由 |
|---|---|---|---|
| `YamlTestDataValidator` | 88% (77/87) | 79% (33/42) | 残り5行は番人コード（スキーマリソース不存在・IOクローズ例外・castList/castMap の非到達 else 分岐）。実稼働環境では到達しない |
| `ValidationError` | 100% | N/A | |

## QAエンジニアレビュー

| 観点 | 判定 | 根拠・改善案 |
|---|---|---|
| 目的に対して意味のあるテスト・動作確認が実施されているか | OK / NG | |
| エッジケースが漏れなくテスト・動作確認されているか | OK / NG | |

## エキスパートレビュー（ソースコード変更タスクのみ）

### 対象言語エキスパートレビュー

| 観点 | 判定 | 根拠・改善案 |
|---|---|---|
| ベストプラクティス準拠 | OK / NG | |
| 既存コードスタイル統一 | OK / NG | |
| テストコードのGWT形式 | OK / NG | |

### ソフトウエアエンジニアレビュー

| 観点 | 判定 | 根拠・改善案 |
|---|---|---|
| 責務分離の適切さ | OK / NG | |
| システム全体の整合性 | OK / NG | |
| 保守性・拡張性 | OK / NG | |

## 総合判定

- 担当者: OK
- QA: OK / NG
- 対象言語エキスパート: OK / NG
- ソフトウエアエンジニア: OK / NG
- ユーザーレビュー可否: 不可（QA・エキスパート・SWEレビュー待ち）
