# T3 完了条件チェック

タスク: 変換ツール parseMessageBlock の構造分離修正
参照文書: 設計書 §12、解説書 §7、Example `05`、仕様リスト MS-01/MS-04
変更対象: `src/main/java/nablarch/test/tool/converter/xls/XlsFormatReader.java`、`tool/converter/model/MessageDataBlock.java`、`tool/converter/yaml/YamlFormatWriter.java`、対応するテスト

## 実施手順（TDD）

1. RED: FW制御ヘッダ＋電文ボディ＋複数データ行を持つ実 messaging Excel（`MessageParserTest.xls`、`MessagingRequestTestSupportTest.xls` の testSuccess 等）を変換し、出力 YAML が下記を満たすことを検証するテストを追加し、現状の失敗（混入・1列ずれ・データ消失）を確認する。
   - `MESSAGE`: `text-encoding` が `directives:`、`requestId`/`userId` が `fw_header:`、電文本文が `records` の `fields`/`rows`
   - `EXPECTED_REQUEST_*`/`RESPONSE_*`: `text-encoding` が `directives:`、`requestId` 等を含む全フィールドが `records` の `fields`/`rows`（`fw_header` は出力しない）
2. GREEN: `parseMessageBlock` の状態機械を、識別行→（ディレクティブ または FW制御ヘッダ の「名前｜値」群）→ 先頭セル `no` のフィールド名称行 → 型行 → 長さ行 → データ行 の順で解釈するよう修正する。`名前｜値` を既知ディレクティブ名と FW制御ヘッダ名（`reader.fwHeaderfields`）に振り分ける。
3. `MessageDataBlock` を `directives`（Map）・`fwHeader`（Map）・`records` に分けて保持する形へ整理する。
4. `YamlFormatWriter` を、`MESSAGE` は `directives:`/`fw_header:`/`records:`、`expected_request_*`/`response_*` は `directives:`/`records:`（`fw_header` なし）で出力するよう修正する。

## 完了条件チェックリスト

| 完了条件 | 担当者判定 | 担当者根拠 | QA判定 | QA根拠 |
|---|---|---|---|---|
| `text-encoding` が `directives:` に出力され `fw_header:`/`fields` に混入しない | OK / NG | | OK / NG | |
| `MESSAGE` の `requestId`/`userId` 等が `fw_header:` マップに出力される | OK / NG | | OK / NG | |
| `expected_request_*`/`response_*` は `requestId` 等も `fields`/`rows` に出力され `fw_header` を出力しない | OK / NG | | OK / NG | |
| フィールド名・型・データ行が正しく対応し1列ずれ・データ消失が起きない | OK / NG | | OK / NG | |
| 出力 YAML がスキーマ `03` に適合する | OK / NG | | OK / NG | |
| 実 messaging Excel 数件で上記が再現確認できる | OK / NG | | OK / NG | |

## QAエンジニアレビュー

| 観点 | 判定 | 根拠・改善案 |
|---|---|---|
| 目的に対して意味のあるテスト・動作確認が実施されているか | OK / NG | |
| エッジケースが漏れなくテスト・動作確認されているか | OK / NG | |

## エキスパートレビュー（ソースコード変更タスクのみ）

### 対象言語エキスパートレビュー

| 観点 | 判定 | 根拠・改善案 |
|---|---|---|
| ベストプラクティス準拠 | OK / NG | |
| 既存コードスタイル統一 | OK / NG | |
| テストコードのGWT形式 | OK / NG | |

### ソフトウエアエンジニアレビュー

| 観点 | 判定 | 根拠・改善案 |
|---|---|---|
| 責務分離の適切さ | OK / NG | |
| システム全体の整合性 | OK / NG | |
| 保守性・拡張性 | OK / NG | |

## 総合判定

- 担当者: OK / NG
- QA: OK / NG
- 対象言語エキスパート: OK / NG / 該当なし
- ソフトウエアエンジニア: OK / NG / 該当なし
- ユーザーレビュー可否: 可 / 不可（理由）
