# P3-4 (#4) Completion Check — 中間モデルの再構築

タスク: Phase 3 #4 / ブランチ `add-yaml`

## 成果物

`src/main/java/nablarch/test/tool/converter/model/` に中間モデル 10 クラスを新設（設計書 §3 クラス図準拠）。形式中立な内部表現で、IN（#6 Excel・#7 YAML）が組み立て、OUT（#8/#9）が消費する。

| クラス | 役割 | 主フィールド |
|---|---|---|
| `TestDataContainer` | テストクラス 1 つ分（ブック／ディレクトリ） | `name`, `sections` |
| `TestDataSection` | 読み込み単位（シート／ファイル） | `name`, `blocks` |
| `TestDataBlock`（abstract sealed; permits ColumnRow/File/Message） | 1 データブロック共通 | `dataType`(本体 `nablarch.test.core.reader.DataType` を再利用), `groupId`, `identifier` |
| `ColumnRowDataBlock`（abstract sealed; permits Table/ListMap） | カラム/行形式の基底 | `columnNames`, `rows` |
| `TableDataBlock`（final） | テーブル（SETUP/EXPECTED/COMPLETE） | （親の継承のみ） |
| `ListMapBlock`（final） | LIST_MAP（dataType 固定） | （親の継承のみ） |
| `FileDataBlock`（final） | 固定長/可変長ファイル | `FileType` enum, `directives`, `records` |
| `MessageDataBlock`（final） | 電文（MESSAGE/REQUEST/RESPONSE 系） | `directives`, `fwHeaderFields`, `records` |
| `RecordLayout`（final） | レコードレイアウト | `recordType`, `fields`, `rows` |
| `FieldDef`（final） | フィールド定義 | `name`, `type`, `length`（すべて String） |

### 設計判断（あるべき姿・memory `new-code-prefer-ideal-design` 準拠）

- **本体 `DataType` 再利用**: 旧モデルと同様 `nablarch.test.core.reader.DataType`（13 種別）を再利用。変換ツール独自 enum を作らない（依存方向 変換ツール→本体 の一方向を維持）。
- **`FieldDef.type`/`length` は形式中立 String**: 旧 Javadoc の「データ型記号（X/N/Z）」表現は採らず、記法のまま保持する旨に修正（steering 決定: YAML 型記法は日本語名称。中間モデルは形式中立に記法保持）。`length` は `"-"`（自動拡張指示）もリテラル保持のため String。
- **`KNOWN_DIRECTIVE_NAMES` を中間モデルに持たせない**: 旧 `MessageDataBlock` 上の静的セットは「name|value 行をディレクティブと FW ヘッダのどちらに振り分けるか」という Excel 読み込み時の分類規則であり、不変データ保持クラスの責務ではない。Excel リーダ（#6）／検証（#11・整合テスト所有）側へ配置を委ね、データモデルは純粋に保つ。
- **leaf を final 化**: 旧 `TestDataContainer`/`TestDataSection`/`RecordLayout` は素の class だったが、継承終端のため `final` 化。sealed 階層の葉（Table/ListMap/File/Message）も `final`。
- **読み取り専用契約を Javadoc 明記**: getter は防御的コピーせず保持参照を返す（`Raw*` 構造層と一貫）。

## Completion Criteria（セルフチェック）

| 完了条件 | 判定 | 根拠 |
|---|---|---|
| 設計書 3 章のクラス図と継承/sealed 関係が一致 | OK | `ModelSealedHierarchyTest` で `TestDataBlock` sealed＋permits={ColumnRow,File,Message}、`ColumnRowDataBlock` sealed＋permits={Table,ListMap}、葉 4 クラス final、継承関係を reflection で実証。図（§3 mermaid）と一致 |
| 単体テスト全 GREEN | OK | `mvn -o test -Dtest='...model.*Test'` → 8 クラス 27 件 Failures=0 Errors=0（QA イテレ1 指摘対応で 20→27 件に増） |
| C0/C1 100%（番人除く） | OK | JaCoCo（`mvn package`→`jacoco:report`）で model パッケージ 11 クラス（10＋nested FileType）すべて INSTRUCTION_MISSED=0／LINE_MISSED=0／BRANCH=0（分岐なし＝C1 充足）。番人コードなし |
| IN 値が記法のまま未加工で保持できる設計 | OK | `TableDataBlockTest`（null セル・`""`・`${systemTime}`・マーカー `[COL]`・小文字テーブル名）、`FieldDefTest`（`"-"`長・日本語型・null）、`MessageDataBlockTest`（`${userId}` FW ヘッダ・順序保持）、`RecordLayoutTest`（recordType=null）で実証 |
| 全データ種別を無損失表現 | OK | SETUP/EXPECTED/COMPLETE_TABLE・LIST_MAP（空行保持）・FIXED/VARIABLE・MESSAGE/REQUEST/RESPONSE 系を各テストで構築・検証。本体 `DataType` 13 種別を網羅 |

- セルフチェック総合: **OK**
- カバレッジ: model パッケージ **C0/C1 100%**（JaCoCo 実測・上表）

## 4 観点レビュー（アーキ/SWE/QA/Java）

独立サブエージェント（会話履歴なし・成果物全文＋完了条件＋設計判断前提を渡す）で実施。前回セッションでアーキ/SWE/Java PASS・QA イテレ1 FAIL（全 13 DataType 網羅漏れ＋エッジ不足）→テスト 20→27 件に増で対応。本セッション（履歴喪失のため）で QA・Java・SWE を再実施し記録。

### QA エンジニア — **PASS**（イテレ2 / agentId `ab5b9b1e38dac9454`）

| 観点 | 判定 | 根拠 |
|---|---|---|
| 完了条件1 クラス図/sealed 一致 | OK | `ModelSealedHierarchyTest` で permits 集合・final・継承を reflection 実証、ソース宣言と一致 |
| 完了条件2 全 GREEN | OK | model 8 クラス `Tests run: 27, Failures:0, Errors:0`（全体ビルドの 4 Error は Mockito 環境起因の既存無関係テスト・model 外） |
| 完了条件3 C0/C1 100% | OK | model 10 クラスは構文上分岐ゼロ（if/else/switch/for/?/&&/catch 不在）・全 16 getter＋4 葉コンストラクタ＋FileType 両分岐到達＝未到達は論理的に存在せず |
| 完了条件4 未加工保持 | OK | `[COL]`/`${...}`/`=`/`-`/null セルと空文字の区別/大文字保持をテストで実証 |
| 完了条件5 全 13 種別網羅 | OK | DataType.java 照合・テスト横断で 13 種別すべて出現・DEFAULT 不使用（Table3+ListMap1+File4+Message5=13）。イテレ1 漏れ完全解消 |
| 意味のある検証 | OK | getter 往復でなく「形式中立な無損失保持」を狙う（null と ""区別・記法リテラル保持・順序・sameInstance で読み取り専用契約固定） |
| エッジケース網羅 | OK | 空テーブル・空行・空 directives/records・null 省略・FW_HEADER 複数レコード順・Map 挿入順・FileType 2 種 |

- 非ブロッカー申し送り: 完了条件 #3 の文言は「JaCoCo で MISSED=0」だが本 pom には jacoco-maven-plugin 未配線（親 pom 継承で `mvn package`→`jacoco:report` 実行可・前回実測済）。分岐ゼロゆえコード上 C0/C1 100% は担保。数値エビデンス継続が必要なら配線検討（合否非該当）。

### Java 言語エキスパート — **PASS**（agentId `a01768458345c985f`）

| 観点 | 判定 | 根拠 |
|---|---|---|
| ベストプラクティス | OK | 全 `private final`＋コンストラクタ代入のみ（不変・スレッドセーフ）。sealed/permits 正・葉 final・抽象基底 abstract。`ListMapBlock` は LIST_MAP 固定コンストラクタで誤指定を型で防止。null 省略契約一貫 |
| コードベース一貫性 | OK | `Raw*`（reader.yaml.model）と高度一貫：パッケージ/import/Javadoc 構成/`@author kiyotis`/getter 注記。`FieldDef`↔`RawFieldDef` はほぼ鏡像。`@Published` 未付与は「内部表現 vs 公開 API」の責務差で妥当（一貫性違反でない） |
| GWT テスト形式 | OK | 全メソッドに Given/When/Then＋日本語名。hamcrest 1.1 制約遵守（iterable matcher 不使用・`new ArrayList<>(keySet())` を `is(List.of(...))`）。matcher は is/nullValue/sameInstance のみ |

### ソフトウェアエンジニア — **PASS**（agentId `a174845ceaf2e6044`）

| 観点 | 判定 | 根拠 |
|---|---|---|
| 関心の分離 | OK | モデルは不変データ保持に徹し読み込み/分類/補完ロジック混入なし。`KNOWN_DIRECTIVE_NAMES`・interpret/マージ不在。階層 Container→Section→Block→RecordLayout→FieldDef が責務妥当。sealed が網羅 switch を支持 |
| システム整合性 | OK | 依存方向 変換ツール→本体 一方向。13 種別→4 ブロック型マッピング無漏れ・無曖昧（3+1+4+5=13、DEFAULT 除外正当）。往復変換(#12)無損失：null/空文字/記法/順序/FW_HEADER 保持。`RawMessage.fwHeader`(Object)→`fwHeaderFields`(Map) は #7 の変換責務境界として整合 |
| 保守性 | OK | 重複は共通基底へ集約・深いネスト/マジックナンバー不在。`FileType` enum で boolean フラグ回避。sealed+final+reflection テストで新ブロック追加時 permits 更新を強制 |

- 非ブロッカー所見: ①`ListMapBlock` の groupId は常に空（LIST_MAP に groupId 概念なし）＝sealed 共通化のトレードオフ・許容。②Javadoc の「MS-04」仕様番号参照はトレーサビリティ良も仕様変更時陳腐化注意。いずれも設計判断範囲で修正不要。

### 総合

- セルフチェック: **OK** ／ QA: **PASS** ／ Java 言語エキスパート: **PASS** ／ ソフトウェアエンジニア: **PASS**
- ブロッカーなし。ユーザーレビューは Operating mode（自律・確認不要）によりスキップ。**#4 完了**。
