# P1-1 Completion Check — #1 本体再利用面の設計書照合・是正

対象タスク: Phase 1 / #1（本体再利用面の設計書照合・是正）
設計書: `docs/pr75/docs/testdata-converter-design.md`（判断 A §70-84 / 共通 §103-114 / 無損失 §31・§49）

## やったこと（要約）

1. **過剰公開の是正**: `getResult`（取り出し口）の可視性 `protected` 化 5 件を **package-private へ revert**。
   - 判断 A §84 が「同一パッケージに相乗りするアダプタが直接呼ぶ」と定めるため、package-private で十分（`protected` は他パッケージのサブクラスにも開く過剰公開）。
   - 対象: `TestDataParsingTemplate`（abstract）・`DataFileParser`・`GroupMessageParser`・`ListMapParser`・`TableDataParser`。
   - revert 後、この 5 ファイルは **main と完全一致（差分ゼロ）**。
2. **再利用 getter（器の中身を読む面）は public のまま確定**（別パッケージ `nablarch.test.tool.converter.*` から器を読むため必須）。設計書 §共通 の表と照合し最小集合であることを確認。
3. **設計書 §共通 の整合是正**: 「いずれも**本体無変更**」は事実誤り（`DataFile`/`DataFileFragment`/`MessagePool` の getter は本ブランチで追加）。
   - `TableData` のみ既存 public（無変更）、他 3 器は「再利用に必要な最小限の**本体変更**」と明記。
   - 設計書 §共通 表に欠落していた `getRecordType` を追加（§31「空欄のレコード種別も無損失で保持」・§49「レコード種別の default 補完は実行しない」が読み取り＝保持を要求するため必須）。
   - 取り出し口（`getResult`・一部 Parser コンストラクタ）は package-private のままとする旨を §共通 に明記（判断 A）。

## 再利用面（最小集合）と各変更の必要理由

| # | 変更 | 可視性 | 設計書根拠 | 必要理由 |
|---|---|---|---|---|
| 1 | `DataFile.getAllFragments()` 追加 | public `@Published` | §共通 表 | 変換ツールが別 pkg からフラグメント群を読む |
| 2 | `DataFile.getDirectives()` 追加 | public `@Published` | §共通 表 | ディレクティブ（文字コード等）を別 pkg から読む |
| 3 | `DataFileFragment` クラス `@Published` 付与 | public（既存）+ 注釈 | §共通 表 | `getAllFragments` の戻り型＝公開 API として明示 |
| 4 | `DataFileFragment.getRecordType()` 追加 | public `@Published` | §31・§49 | レコード種別を無損失で保持するため読み取り必須（表へ追記） |
| 5 | `DataFileFragment.getNames/getTypes/getLengths/getValues()` 追加 | public `@Published` | §共通 表 | フィールド定義・データ行を別 pkg から読む |
| 6 | `MessagePool.getFwHeader()` package-private→public | public `@Published` | §共通 表 | FW 制御ヘッダを別 pkg から読む |
| 7 | `getResult` ×5 の可視性 | **package-private へ revert** | §84 | 同一 pkg アダプタが直接呼ぶ＝過剰公開を是正（差分ゼロ化） |

- いずれも `@Published(tag = "architect")`。本リポジトリの既存規約（main で 59 箇所、全て architect タグ）と一致。
- 防御的コピー（`Collections.unmodifiableList/Map`）で読み取り専用を担保。
- **スコープ外**: `YamlTestDataParser` および `reader/yaml/*`（YamlFileBuilder/YamlLoader/YamlMessageBuilder/YamlSection/YamlTableDataBuilder）は ① YAML 本体対応の実装本体であり、構造／値加工の 2 層分離は **#2** で扱う。

## Completion Criteria

| Criterion | Self-check | Evidence |
|---|---|---|
| 本体変更が「再利用に必要な最小集合」に一致し各変更の必要理由を 1 件ずつ列挙できる | OK | 上表 7 件。取り出し口は package-private へ収束（差分ゼロ）、読み取り getter のみ public で残置。各行に設計書根拠を明示 |
| `git diff main..HEAD`（＋本作業の working tree）の本体差分が設計書根拠つきで説明可能 | OK | 上表で全件説明。再利用面以外（YAML 実装本体）は #2 スコープと切り分け |
| reader 既存テストが全 GREEN（振る舞い不変） | OK（要注記） | 下記参照 |

### reader テスト結果（クリーン実行・本変更あり）

- reader パッケージ **202 件中 198 件 GREEN**。
- 変更が影響し得る全テスト（`TestDataParsingTemplateTest` 6・`SingleDataParsingTemplateTest` 1・`FixedLengthFileParserTest` 1・`VariableLengthFileParserTest` 2・`BasicTestDataParserTest` 16・`DbLessTestDataParserTest` 3・`SendSyncMessageParserTest` 1・`YamlTestDataParserTest` 43 ほか）は **全 GREEN**。
- **振る舞い不変の証明**: 本変更は可視性のみ（実行ロジック追加なし）。`git stash` でベースライン（本変更なし）と比較し、結果が完全一致することを確認。

### 残存 RED（4 件・本タスク起因ではない・スコープ外）

- `nablarch.test.core.reader.yaml.YamlTableDataBuilderTest`: 4 errors
  - 内容: `QuotationTrimmer` が値 `["]` で `StringIndexOutOfBoundsException`（YAML の**値加工層**のバグ）。
  - **本変更前から発生**（stash ベースラインで同一の 4 errors を確認）＝可視性 revert とは無関係。
  - 該当は YAML 値加工（特殊記法解釈）の不具合であり、**#2（本体 YAML 読み込みの 2 層分離）**で構造／値加工を分離する際に解消すべき領域。Phase 1 完了ゲート前に GREEN 化が必要。

### カバレッジ

- #1 は実行ロジックを追加せず（可視性の縮小＋設計書整合のみ）、新規分岐・行なし → 行/分岐カバレッジ測定は N/A。
- 既存の reader テスト群が公開 getter（既追加分）を実行している。

## Overall Verdict

- Self-check: **OK**（reader テストは振る舞い不変を証明。残存 4 RED は本タスク起因でなく #2 スコープと明記）
- フェーズレビュー（4 観点サブエージェント＋ユーザー）: Phase 1（#1+#2）完了時に実施予定。
- Ready for commit (#1): **Yes**
