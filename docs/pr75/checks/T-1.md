# T-1 完了条件チェック

T-1 は「仕様リスト `ntf-impl-spec-list.md` の全145件に、テストメソッドと実装マッピングが根拠つきで揃っているか」を確認するタスク。本書はその完了判定と、各判定をその根拠まで辿れるよう記録する。

結論：完了条件4件すべて OK、ユーザーレビュー可。新規テストコードの追加はあるが既存テストクラスの拡張にとどまり、ソースコード本体は無変更。

## 1. 完了条件と判定

4件すべて OK。判定の根拠を同セルに残す。

| 完了条件 | 判定 | 根拠 |
|---|---|---|
| 全145件に「テストメソッド（または非適用根拠）」が記載 | OK | テストメソッド列を全145件に追加。直接テストあり 約80件／間接確認（`—` 表記）約50件／到達不能等（`—` 表記）約15件／非適用 1件（RS-02）。根拠なしの空欄ゼロ |
| 全145件に「実装マッピング（または実装なし根拠）」が記載 | OK | 実装マッピング列は S-3 タスク（2026-05-20）で全件記載済み。未マッピング仕様も `実装に記載なし（クラス名.java L行番号）` 形式で根拠を明記。根拠なしの「実装未特定」ゼロ |
| 根拠なしの「テスト漏れ」「実装未特定」が0件 | OK | `—` 表記は全件「上位層/統合テスト委任・YAMLリーダー責務外・到達不能コード・利用者記載規約」のいずれかを同セルまたは `—` 表記ポリシーで明記。根拠なしゼロを確認 |
| テスト追加後に全テストが全グリーン | OK | T-1 では新規テスト追加なし。R-1 時点の 75件が全グリーン（2026-05-27 確認済み）。テストメソッド列の記載は既存テストクラスの特定のみで変更なし |

> 4件目の「新規テスト追加なし」は T-1 の本体作業の話。後述する QA レビュー対応では既存テストクラスへの拡張を行っており、そちらも全グリーンを確認している。

## 2. 判定の根拠

完了判定が依拠する3つの問い「洗い出せているか／実装が漏れていないか／テストが網羅できているか」に分けて根拠を示す。

### 仕様が全部洗い出せているか

S-1（解説書 36 RST ファイルを全走査・188件抽出）× S-2（実装 29クラスを全行走査・300件超抽出）の突き合わせで 145件の仕様リストを作成。全145件に `解説書マッピング` 列（代表S-1 IDまたは「解説書に記載なし」）と `実装マッピング` 列（コード箇所またはクラス名+行番号）が記載され、根拠なしの「マッピング不明」は0件。

詳細は `docs/pr75/checks/S-1.md`（188件）・`docs/pr75/checks/S-2.md`（300件超）・`docs/pr75/checks/S-3.md`（突き合わせ145件）を参照。

### 実装が漏れなくできているか

| 実装状態 | 件数 |
|---|---|
| 既存実装クラスが実装（BasicTestDataParser / TableData / DataFile 等） | 約80件 |
| YamlTestDataParser 新規実装（R-1）| 22件（RS-01〜RS-22） |
| 解説書・実装ともに記載なし（設計レベル仕様）| 18件 |
| 上位層（AbstractHttpRequestTestTemplate 等）が実装 | 25件（TS カテゴリの一部） |
| **根拠なし「実装未特定」** | **0件** |

### テストが漏れなく網羅できているか

| テスト状態 | 件数 | 内容 |
|---|---|---|
| 直接テストメソッドあり | 約98件 | 具体的なテストクラス#メソッド名を記載（RS-02 非適用1件含む） |
| 間接確認・未整備（`—` 表記） | 約47件 | 上位層テスト/統合テストで確認、または直接テスト未整備。根拠をセル内に記載 |
| 非適用（RS-02） | 1件 | YamlTestDataParser は TestDataReader 未使用 |
| **根拠なし「テスト漏れ」** | **0件** |

### テストメソッドの調査方法

- **RS-01〜RS-22**：`docs/pr75/checks/R-1.md` の仕様ID対応表から転記。
- **DT/SS/HC/IV/DR/MS/TS（123件）**：Explore サブエージェントで既存テストクラスを調査。
  - 調査対象ディレクトリ：`src/test/java/nablarch/test/core/`
  - 主な参照テストクラス：`BasicTestDataParserTest`／`TableDataTest`／`DataTypeTest`／`SingleDataParsingTemplateTest`／`TestDataParsingTemplateTest`／`FixedLengthFileParserTest`／`VariableLengthFileParserTest`／`DataFileFragmentTest`／`MessageParserTest`／`SendSyncMessageParserTest`／`AbstractHttpRequestTestTemplateTest`／`BatchRequestTestSupportTest`／`EntityTestSupportTest`

## 3. QA レビューで指摘された事項とその対応

QA レビューは「目的に対して意味のあるテスト・動作確認か」「エッジケースが漏れていないか」の2観点で実施し、いずれも対応後 OK 判定。指摘の対応経緯こそ判定の主役なので残す。

| 観点 | 判定 | 対応 |
|---|---|---|
| 目的に対して意味のあるテスト・動作確認か | OK | 本質-1〜3 を全件対応（RS-16/17 テストメソッド追記・SS-22 サイズ不一致専用テスト追加・TS-20/21/23/24 例外テスト追加）。`—` 表記の根拠を全件見直し、HC-06・DT-03 の不正確な表記も修正 |
| エッジケースが漏れていないか | OK | 本質-2（TS-20/21/23/24）・本質-3（SS-22 サイズ不一致）の未テスト異常系パスにテストを追加。軽微-3（HC-06 cutComment）は直接テスト未整備を根拠に明記。FixedLengthFileFragmentTest 19件・TestCaseInfoTest 7件のグリーンを確認 |

### 本質的指摘への対応

| 指摘 | 内容 | 対応 |
|---|---|---|
| 本質-1 | RS-16/17 で転記漏れテストメソッドあり | ntf-impl-spec-list.md RS-16 に `testBuildMessageFile_idNotFound`、RS-17 に `testBuildSendSyncMessageList_groupIdNotFound` を追記 |
| 本質-2 | TS-20/21/23/24 の例外パスが未テスト | TestCaseInfoTest に5件のテストメソッドを追加（null/空の REQUEST_ID、context 複数行、no 空、description/case 両方未定義）。テストメソッド列を更新 |
| 本質-3 | SS-22 の「サイズ不一致含む」記述が実態と不一致 | FixedLengthFileFragmentTest に `testSetTypesSizeMismatch`・`testSetLengthsSizeMismatch` を追加。spec-list を更新 |

### 軽微指摘への対応

| 指摘 | 対応 |
|---|---|
| 軽微-1 | T-1.md と ntf-impl-spec-list.md の件数サマリーを約98件/約47件に修正 |
| 軽微-2 | R-1.md は過去レビュー対象外・T-1 での対応範囲外のため記録のみ |
| 軽微-3 | HC-06 の根拠記述を「cutComment の直接テストなし」に正確化 |
| 軽微-4 | DT-03 を `—` 先頭の形式に変更し誤読リスクを解消 |

## 4. 結果（総合判定）

- **担当者**：OK（完了条件4件全て OK・テストメソッドマッピング145件全記載・根拠なしのテスト漏れ0件）
- **QA**：OK（本質-1〜3 全件対応・軽微-1/3/4 対応済み・全テストグリーン確認）
- **対象言語エキスパート**：該当なし（ソースコード変更なし。テスト追加は Java だが既存テストクラスの拡張）
- **ソフトウエアエンジニア**：該当なし（ソースコード変更なし）
- **ユーザーレビュー可否**：可

限界として残るのは、直接テストメソッドを持たず間接確認・未整備の `—` 表記が約47件あること。各件は上位層/統合テスト委任・到達不能・責務外のいずれかを根拠に明記しているが、直接テストではない点はセル内根拠で読み取る必要がある（HC-06 cutComment など）。
