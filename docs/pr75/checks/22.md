# #22 Completion Check

**対象**: `MasterDataSetUpper` のYAML形式マスタデータファイル対応（事象4後半）
**基準コミット**: `786c23c`
**コミット**: `a94fa08`（再現テストのみ）→ `3de41ff`（修正）
**実測日**: 2026-08-21

行番号はすべて `3de41ff` 時点のもの。

---

## Completion Criteria

| Criterion | 判定 | Evidence |
|---|---|---|
| YAML形式相当のマスタデータファイルを指定した場合、そのファイル内の**複数**テーブルデータが投入されることが自動テストで示されている | OK | `MasterDataSetUpperTest#testSetUpMasterDataFromNonExcelFile`（`src/test/java/nablarch/test/core/db/MasterDataSetUpperTest.java:171`）が、`SETUP_TABLE` ブロックを2つ持つタブ区切りテキスト `src/test/resources/nablarch/test/core/db/masterdata/MASTER_DATA_NON_EXCEL.txt`（`GRANPA` 3行・`STRANGER` 2行）を `MasterDataSetUpper.main` に渡し、投入後に `VariousDbTestHelper.findAll(Granpa.class).size() == 3` と `findAll(Stranger.class).size() == 2` を表明する。<br>行数を既存 fixture と変えてある（`MASTER_DATA2.xls` の `GRANPA` は2行）ため、既存 fixture の混入では通らない。<br>1リソースから複数の `TableData` が採れるのは、`TableDataParser` が `GroupDataParsingTemplate` を継承し `shouldStopOnNextOne()` が常に `false` を返す（`GroupDataParsingTemplate.java:51-53`）ためで、`doParse`（`TestDataParsingTemplate.java:284-309`）が2つ目以降の `SETUP_TABLE=` 行でも打ち切らない |
| Excel形式（`.xls`）のマスタデータファイルは、シート列挙を含め既存と同じコードパスで処理され、既存テストに変化がない | OK | `getAllTableData`（`MasterDataSetUpper.java:185-204`）で `isExcelFile` が真のとき通る経路は、`PoiXlsReader.getSheetNames` → シートごとに `parser.getSetupTableData(dir, "<拡張子なしファイル名>/<シート名>")` で修正前と同一。差分は `getMasterFileNameWithoutSuffix` の呼び出しをループ外へ引き上げた点のみ（ゲート7 の diff 参照）。<br>既存テスト6件のソースに差分なし（ゲート6）。全件 `BUILD SUCCESS`（ゲート3） |
| Excel形式＋YAML用パーサという取り違えケースの挙動（無言0件）は変更されていない | OK | **修正前・修正後の両方を実測した。** 一時テスト（`TmpMismatchCheckTest`。確認後に削除済み）で、`testDataParser` のリーダを `TsvTestDataReader` に差し替えたリポジトリのもと `getAllTableData(new File(".../MASTER_DATA.xls"))` を呼び、標準出力へ件数を出した。<br>修正前（`786c23c` の `MasterDataSetUpper.java`）: `###MISMATCH_RESULT### size=0` / `BUILD SUCCESS`<br>修正後（`3de41ff`）: `###MISMATCH_RESULT### size=0` / `BUILD SUCCESS`<br>いずれも例外なく0件。`.xls` は `isExcelFile` が真なのでシート列挙へ行き、`TsvTestDataReader.isDataExisting` が `MASTER_DATA/SON.txt` を見つけられず `BasicTestDataParser.getSetupTableData`（`:50-57`）が無言で空リストを返す |

---

## 実装

**変更は `MasterDataSetUpper.java` の1ファイルのみ。`getAllTableData` の中とその private ヘルパに閉じている。**

- `getAllTableData`（`:185-204`）の冒頭で `isExcelFile(masterDataFile)` を判定し、Excel形式以外は `parser.getSetupTableData(dir, fileNameWithoutSuffix)` を1回だけ呼んで返す（`:190-194`）
- 渡すリソース名は**拡張子を除いた名前**。既存の `getMasterFileNameWithoutSuffix`（`:231`）を使う。拡張子はリーダ／パーサ側が付与する責務であるため（`TsvTestDataReader.java:84-86` が `new File(path, dataName + ".txt")`、`nablarch-testing-yaml` の `YamlLoader.java:83-85` が `basePath + "/" + resourceName + ".yaml"`）、拡張子付きで渡すと `MASTER_DATA.yaml.yaml` を探しに行く
- Excel と判定する拡張子は `EXCEL_EXTENSIONS`（`:207`）= **小文字の `xls`／`xlsx` のみ**。`PoiXlsReader.isAcceptableFilename`（`:283-295`）の `extension.equals("xls") || extension.equals("xlsx")` と同じ定義に揃えた（判断の理由は下記「拡張子の判定」）
- リソースが存在しないときに無言で空リストを返す挙動（`BasicTestDataParser.java:50-57`）は変更していない。事象4前半として現状維持と判定済み
- **公開 API の変更は0件。** 追加したのは private な `EXCEL_EXTENSIONS` と `isExcelFile` のみ。`getAllTableData` は修正前後とも package private

---

## ゲート実測（2026-08-21）

| # | ゲート | 結果 | 実測値 |
|---|---|---|---|
| 1 | 再現テストが RED | OK | 下記 |
| 2 | 修正後 GREEN | OK | 下記 |
| 3 | 全件テスト | OK | `Tests run: 854, Failures: 0, Errors: 0, Skipped: 7` / `BUILD SUCCESS` |
| 4 | Skipped の内訳が不変 | OK | 下記 |
| 5 | 負のコントロール（変異） | OK | 下記 |
| 6 | Excel 経路の非変更 | OK | 既存6件のソースに差分なし。差分は import 1行と新規メソッド1つの追加のみ |
| 7 | 公開 API の宣言差分が無い | OK | `git diff 36cabc6 HEAD -- src/main/ \| grep -E '^[+-]' \| grep -E '\b(public\|protected)\b'` → 出力なし |
| 8 | 作業ツリー | OK | `git status --porcelain` が空。コミットは `a94fa08`（再現テスト）と `3de41ff`（修正）の2本 |

### ゲート1 — 再現テストが RED（`a94fa08`、修正前）

`mvn -o test -Dtest=MasterDataSetUpperTest`

```
[ERROR] Tests run: 7, Failures: 0, Errors: 1, Skipped: 0, Time elapsed: 7.624 s <<< FAILURE! - in nablarch.test.core.db.MasterDataSetUpperTest
[ERROR] testSetUpMasterDataFromNonExcelFile(nablarch.test.core.db.MasterDataSetUpperTest)  Time elapsed: 0.152 s  <<< ERROR!
java.lang.RuntimeException: test data file open failed.
	at nablarch.test.core.db.MasterDataSetUpperTest.testSetUpMasterDataFromNonExcelFile(MasterDataSetUpperTest.java:176)
Caused by: java.lang.IllegalArgumentException: Your InputStream was neither an OLE2 stream, nor an OOXML stream
	at nablarch.test.core.db.MasterDataSetUpperTest.testSetUpMasterDataFromNonExcelFile(MasterDataSetUpperTest.java:176)
```

**例外の型・メッセージともレビュー役の事前実測と一致した**（`RuntimeException: test data file open failed.` / cause `IllegalArgumentException: Your InputStream was neither an OLE2 stream, nor an OOXML stream`）。既存6件は同時に成功している（`Tests run: 7` / `Errors: 1`）。

### ゲート2 — 修正後 GREEN（`3de41ff`）

```
[INFO] Tests run: 7, Failures: 0, Errors: 0, Skipped: 0, Time elapsed: 4.727 s - in nablarch.test.core.db.MasterDataSetUpperTest
[INFO] BUILD SUCCESS
```

### ゲート3・4 — 全件テスト

`mvn -o clean test`

```
[WARNING] Tests run: 854, Failures: 0, Errors: 0, Skipped: 7
[INFO] BUILD SUCCESS
```

基準 `853`（`36cabc6` 時点。レビュー役実測）＋ 追加1件 = `854`。追加したテストは `testSetUpMasterDataFromNonExcelFile` の1件のみ。

Skipped 7件の内訳（実測。基準と不変）:

```
[WARNING] Tests run: 4,  ... Skipped: 2 ... - in nablarch.test.tool.sanitizingcheck.HtmlConvertTest
[WARNING] Tests run: 22, ... Skipped: 2 ... - in nablarch.test.core.messaging.RequestTestingMessagingClientTest
[WARNING] Tests run: 15, ... Skipped: 2 ... - in nablarch.test.core.db.DbAccessTestSupportTest
[WARNING] Tests run: 15, ... Skipped: 1 ... - in nablarch.test.core.util.FileUtilsTest
```

`2 + 2 + 2 + 1 = 7`。

### ゲート5 — 負のコントロール（変異）

**2種類の変異を入れ、分岐の両方向がテストで固定されていることを実測した。** どちらも実測後に復元し、復元後 `grep -n "MUTANT"` が0件、`git status --porcelain` が空であることを確認済み。

**変異1（指示書が要求したもの）— 追加した分岐を無効化する**

`if (!isExcelFile(masterDataFile)) {` → `if (false) {`（＝常に従来のシート列挙へ行く）

```
[ERROR] Tests run: 7, Failures: 0, Errors: 1, Skipped: 0
[ERROR]   MasterDataSetUpperTest.testSetUpMasterDataFromNonExcelFile:176 » Runtime test ...
Caused by: java.lang.IllegalArgumentException: Your InputStream was neither an OLE2 stream, nor an OOXML stream
```

→ **追加テストだけが落ち、既存6件は通る。** 空振りではない。

**変異2（追加で実施）— 条件を反転し、Excel形式を非Excel経路へ流す**

`if (!isExcelFile(masterDataFile)) {` → `if (isExcelFile(masterDataFile)) {`

```
[ERROR] Tests run: 7, Failures: 0, Errors: 5, Skipped: 0
[ERROR]   MasterDataSetUpperTest.testMain:110 » IllegalArgument invalid data name. [MAST...
[ERROR]   MasterDataSetUpperTest.testSetUpInOrder:143->doSetUp:192 » IllegalArgument inv...
[ERROR]   MasterDataSetUpperTest.testSetUpInOrderWithBackUp:161->doSetUpWithBackUp:199 » IllegalArgument
[ERROR]   MasterDataSetUpperTest.testSetUpMasterData:77 » IllegalArgument invalid data n...
[ERROR]   MasterDataSetUpperTest.testSetUpMasterDataFromNonExcelFile:176 » Runtime test ...
```

→ マスタデータを実際に投入する既存4件が全滅する。**`isExcelFile` が `.xls` に対して真を返すこと（Excel側）も既存テストで固定されている**ことを示す。変異1だけでは非Excel側しか担保できないため追加した。

### ゲート6 — Excel 経路の非変更

`git diff a94fa08^ HEAD -- src/test/java/nablarch/test/core/db/MasterDataSetUpperTest.java` の差分は次の2つだけで、**既存の6メソッドおよび `setUp`/`beforeClass`/`doSetUp`/`doSetUpWithBackUp` に変更行は1行も無い**。

- `import nablarch.test.RepositoryInitializer;` の1行追加（`:10`）
- `testSetUpMasterDataFromNonExcelFile` の追加（`:166-188`。Javadoc を含む）

既存 fixture（`MASTER_DATA.xls`・`MASTER_DATA2.xls`、`src/test/java/` 配下の同名2件）にも変更はない。追加した fixture・設定は新規2ファイルのみ。

### ゲート7 — 公開 API の宣言差分が無い

`git diff 36cabc6 HEAD -- src/main/` の変更は `MasterDataSetUpper.java` 1ファイル。追加された宣言は

- `private static final List<String> EXCEL_EXTENSIONS`（`:207`）
- `private boolean isExcelFile(File)`（`:215`）

の2つで、いずれも `private`。`public`／`protected` を含む追加・削除行は0件（grep 実測）。`getAllTableData` は修正前後とも package private のまま。

---

## 拡張子の判定 — 小文字の `xls`／`xlsx` に揃えた

`PoiXlsReader.isAcceptableFilename`（`:283-295`）と同じ定義を採った。大小無視にせず、`xlsm` 等も足していない。NTF が Excel テストデータとして認識する拡張子は既に小文字の `xls`／`xlsx` だけに決まっており（`PoiXlsReader.java:264-267` の `isDataExisting` は `fileName + ".xls"` → `+ ".xlsx"` しか見ない）、ここで別定義を作ると NTF 内に Excel の定義が2つできるため。

### 記録すべき事実（「影響なし」ではない）

**`MasterDataSetUpper` は修正前、拡張子を一切見ずに POI へ渡していた**（`786c23c` 時点の `MasterDataSetUpper.java:185`）。POI が中身で形式を判定するため、`.xlsm` や大文字 `.XLS` のマスタデータファイルも開けていた（レビュー役が `MASTER_DATA.xls` を `.xlsm`／`.XLS` にコピーして実測し、どちらも `[FATHER, SON]` を返した）。

本修正により、**それらは非Excel経路へ落ちる。** リーダが対応する形式でなければ、無言で0件になる。

本リポジトリの実データは **`xls` 59件・`xlsx` 2件、`xlsm`・`xlsb`・大文字拡張子は0件**（Git 追跡ファイルの全数。出典コマンドは `git ls-files | grep -iE '\.(xls|xlsx|xlsm|xlsb)$'`。2026-08-21 に本セッションで実測）。<br>※ 以前ここに書いていた「`xls` 523件・`xlsx` 18件」はレビュー役が出した誤りで、どの走査範囲でも再現しない（Git 追跡 59/2、作業ツリーで `target` 配下を除くと 295/10、含めると 580/20）。`find` は `target` 配下のビルド生成物と `.claude/worktrees/` 配下の隔離 worktree を拾うため走査範囲が一意に決まらず、出典に使わない。**`xlsm`・`xlsb`・大文字拡張子が0件である点は3つの走査範囲すべてで一致しており、本節の結論は変わらない。**

**対象PJのテストデータは未走査である。** 本リポジトリで該当0件であることは、対象PJに該当が無いことを意味しない。

---

## 未カバーの項目

- **YAML パーサ実体での確認は行っていない。** 本リポジトリは `nablarch-testing-yaml` に依存していないため、「YAML形式相当」として `TsvTestDataReader`（`src/test/java/nablarch/test/core/reader/TsvTestDataReader.java`。テスト専用）を `testDataParser` のリーダに差した設定で検証した。これは `#21` が採った方法と同じ（`src/test/resources/nablarch/test/core/messaging/directory/directory-component-configuration.xml:20-25`）。実際の `.yaml` ファイルで `MasterDataSetUpper` を動かす確認は本リポジトリでは行えない
- **拡張子なしファイル・大文字拡張子・`xlsm` を `MasterDataSetUpper` に渡すケースの自動テストは無い。** 判定ロジック（`isExcelFile`）の分岐のうち、テストで固定されているのは「`.xls` → Excel経路」「`.txt` → 非Excel経路」の2つ。拡張子なし（`dotPosition < 0`）の分岐は未テスト
- **複数ファイル指定時に Excel と非Excel を混在させるケースは未テスト。** `getAllTableDataInFiles`（`:118-124`）はファイルごとに `getAllTableData` を呼ぶため混在は成立するはずだが、実測していない

---

## 判断事項

### (a) 直した

- **指示書 `steering.md:174` の Steps は「`parser.getSetupTableData(dir, masterFileName)`」と書いているが、拡張子を除いた名前を渡すのが正しい。** 指示書 §2 が既にこの点を訂正しており、それに従った。拡張子付きで渡すとリーダ側が二重に拡張子を付ける
- **ゲート5 の変異を1種類ではなく2種類実施した。** 指示書が要求した変異1（分岐の無効化）だけでは、`isExcelFile` が `.xls` に対して真を返すこと（Excel側）が固定されているかを示せないため、条件反転の変異2を追加した
- **Completion criteria 3（取り違えケースの無言0件）を、修正前・修正後の両方で実測した。** コードを読んで「変わっていないはず」と書くのではなく、一時テストで件数を実測して両方 `size=0` であることを確認した（一時テストは削除済み）

### (b) 直すと別の前提を壊す

- なし

### (c) ユーザー判断が要る

- **対象PJに `.xlsm`・`.xlsb`・大文字拡張子のマスタデータファイルが無いかの確認。** 本リポジトリは0件だが対象PJは未走査であり、本リポジトリからは確認できない。走査するかどうかはユーザー判断

---

## 後始末

`MasterDataSetUpper.main` は `SystemRepository.clear()` してから設定を読み直す（`MasterDataSetUpper.java:98-102`）。追加テストはリポジトリをタブ区切りテキスト用のリーダを持つ設定へ差し替えるため、try/finally で `RepositoryInitializer.recreateRepository("unit-test.xml")` を呼んで戻している（`MasterDataSetUpperTest.java:183-187`）。

**`RepositoryInitializer.revertDefaultRepository()` は使えない。** 同メソッドは `isReInitialized` が真のときしか動かない（`RepositoryInitializer.java:47-53`）が、`MasterDataSetUpper.main` は `RepositoryInitializer` を経由せず自前の `initializeRepository` でリポジトリを差し替えるためこのフラグが立たず、無音で何もしない。前例（`RepositoryInitializerTest.java:34-50`）と形は同じだが、呼ぶメソッドは変えている。

---

## 併せて行った記録の掃除（指示書 §7.3）

1. **State の Notes から「未 push のコミットが N 本」という記述を削除した。** `786c23c` が「6本→2本」に直したが、その修正コミット自身が3本目になって直後に再びずれた（本セッション着手時の実測は `ahead 3`）。**数を書くと書いた瞬間に古くなる**ため、origin の SHA と実測コマンドだけを残す形に変えた。<br>元の「6本」という記述は**レビュー役が誤った前提を指示書に書いたことに由来する**（`36cabc6` を基準とした指示書 §0 は `ahead 1` と実測しており、6本という値の出所は指示書側の誤りである）
2. **`checks/23.md`「ユーザー判断を仰ぐ事項」の2番と、State Notes の「未決のユーザー判断が1件」を決定事項として書き直した。** 2026-08-21 のユーザー判断で「`#23` のレビュー（QA・Craft・Verification）は回さず、テストを1件足す」と確定し、そのテスト（`AssertionTest#testAssertTableEqualsWithEmptyColumnNamesOnEmptyTable`）は `36cabc6` で入っている
