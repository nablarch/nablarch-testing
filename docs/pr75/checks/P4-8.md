# P4-8 Completion Check — #8 YamlFormatWriter 再構築（YAML OUT）

対象: `src/main/java/nablarch/test/tool/converter/TestDataFormatWriter.java`（新設・IF）／
`src/main/java/nablarch/test/tool/converter/yaml/YamlFormatWriter.java`（新設）／
`src/test/java/nablarch/test/tool/converter/yaml/YamlFormatWriterTest.java`（新設・30 件）。本体ゼロ差分。

## Completion Criteria

| Criterion | Self-check | Evidence |
|---|---|---|
| 全種別を YAML 出力 | OK | TABLE3（setup/expected/complete）・LIST_MAP・FILE（fixed/variable）・MESSAGE・送信系4種を `serialize` テストで出力検証。`DataType`→セクションキーは writer 内 `sectionKey` で全 13 種を網羅（`DEFAULT` は不正入力として `IllegalArgumentException`） |
| 記法（#7 IN と対称） | OK | 全値ダブルクォート（`"${u}"`/`""`）・明示 null はアンクォート `null`・キーは原則無クォート（特殊文字/空白/空文字/先頭インジケータのみクォート）・group_id は整形 `[x]`→生値・`table:`/`id:`/`path:`/`type:`・`fields:` フロー map（type/length null は省略）・`rows:` フロー list・`fw_header:`・FW_HEADER レコード非出力（送信系は fw_header なし＋"no" 保持） |
| 単体テスト GREEN | OK | `YamlFormatWriterTest` 30 件 0F/0E |
| 往復（記法対称の実証） | OK | モデル→`write`→実 `YamlFormatReader`→モデルの往復を TABLE/固定長 FILE/MESSAGE/送信系の 4 経路で同値確認（実 SnakeYAML パース・実ビルダ経由） |
| C0/C1 100%（番人除く） | OK | JaCoCo 実測 instr **99%**（825 中 8 missed）／branch **96%**（104 中 4 missed）。未到達は全て防御的番人（下記） |
| 本体無変更 | OK | `git diff main..HEAD -- src/main/java/nablarch/test/core` 空（新規は converter 配下のみ） |

### 番人（C0/C1 対象外・到達不能/防御）

- `emitBlock` の `else throw`（L130/133）: `TestDataBlock` は sealed（permits Table/ListMap/File/Message の 4 種）で第 5 の派生型を構築できず到達不能。
- `write` の `if (parent != null)`（L70）の false 枝: 出力先がルート相当でない限り親は常に非 null。false を踏むにはカレントディレクトリへ書く必要があり副作用回避のため未到達。
- `isPlainSafeKey` の `c < 0x20`（L399）の枝: キー（カラム名/ディレクティブ名等）に制御文字は出現しない防御。
- `rawGroup` の `charAt(last) == ']'`（L432）false 枝: `"[x"` のような不均衡角括弧は中間モデルが生成しない防御（`"[x]"` 均衡・`""`・生値 `"raw"` は網羅）。

## QA Engineer Review

| Aspect | Verdict | Evidence |
|---|---|---|
| 仕様を意味する検証 | OK | 全種別（TABLE3/LIST_MAP/FILE fixed,variable/MESSAGE/送信系4種）を `serialize` で出力検証。記法対称・全値クォート・null区別・列順保持・マーカー/FW_HEADER除外・group_id生値化・"no"保持・length/type省略を個別検証 |
| エッジケース網羅 | OK | 空セクション/空行/空カラム/行不足null補完/空fields,rows/null type省略/クォート・エスケープ（`"` `\` 改行 復帰 タブ 0x01）/null vs "null"/特殊文字キー・空キー・先頭インジケータ/不正DataType/write I/O例外 |

QA 指摘（PASS・条件付き）→ **全件対応済**:
- NG-1 前後空白値の往復未実証 → `roundTrip_leadingTrailingWhitespace_isPreservedThroughRealReader` 追加（半角/全角前後空白を実 Reader で往復し脱落しないこと）
- NG-2 null/"null"/数値文字列の往復区別未実証 → `roundTrip_nullAndNullStringAndNumeric_areDistinguishedThroughRealReader` 追加（実 Reader 読み戻しで null と "null"/"123" が区別保持）

## Expert Reviews

### Language Expert

| Aspect | Verdict | Evidence |
|---|---|---|
| ベストプラクティス | OK | 命名/例外ラップ（UncheckedIOException）/null安全/final不変/`@Override`+`{@inheritDoc}`/可視性 妥当。手書きエミッタの `\xNN` 制御文字エスケープが SnakeYAML Engine 3.0.1（YAML 1.2）で読み戻せることを実機検証＝IN 整合 |
| 既存コードスタイル整合 | OK | `YamlFormatReader` 基準でジェネリクス明示型引数・`instanceof`連鎖・Javadoc・セクションコメントが統一。`sectionKey` の enum switch は妥当な使い分け |
| GWT テスト形式 | OK | Given/When/Then コメント・1テスト1意図・自己記述的メソッド名・往復4経路実証 |

Language 指摘（PASS・非ブロッカー）→ **全件対応済**:
- `@After YamlLoader.clearCacheForTest()` 未呼び出し → `clearLoaderCache()` 追加（将来同一パステストのキャッシュ汚染予防）
- `org.junit.Assert.fail`/`java.io.UncheckedIOException` の FQCN → import 化

### Software Engineer

| Aspect | Verdict | Evidence |
|---|---|---|
| 責務分離 | OK | `serialize`（純粋・I/O無）と `write`（I/Oのみ）分離。`Seq` が `- ` 配置を1箇所に集約。手書きエミッタ採用は出力が狭い部分集合ゆえ妥当 |
| システム整合（IF 契約・API 互換） | OK | `write(container,basePath)` は `read(basePath,resourceName)` と対称。出力キーが reader の `FIELD_*`/`KEY_*` 定数と1:1＝往復可逆を構造保証。`sectionKey` はキー文字列を本体定数共有でドリフトなし（写像のみ重複・本体メソッドは全13種未網羅で流用不可） |
| 保守性 | OK | emit メソッド群の抽象化レベル均一・番人4枝は構造的到達不能（sealed 階層実証） |

SWE 指摘（PASS・非ブロッカー）→ **対応済**:
- `Seq` の `level + 2` 散在（4箇所）→ `Seq.childLevel()` 抽出で意味を1箇所集約

## 最終カバレッジ（指摘対応後・JaCoCo 実測）

- instr **99%**（174行中 missed 1）／branch **96%**（missed complexity 4＝番人4枝）／methods 0 missed（`childLevel` 含む全21メソッド網羅）
- 番人4枝は P4-8 既載のとおり構造的到達不能（emitBlock sealed default・write parent==null・isPlainSafeKey 制御文字キー・rawGroup 不均衡角括弧）

## Overall Verdict

- Self-check: OK
- 回帰: OK（`YamlFormatWriterTest` 32件 0F/0E＝30→+往復2件。指摘対応後も本体ゼロ差分）
- QA: **PASS**（NG-1/NG-2 往復テスト追加で対応）
- Language expert: **PASS**（@After/import 対応）
- Software engineer: **PASS**（childLevel 抽出）
- Ready for user review: **Yes**（3観点全 PASS・指摘全件対応・回帰GREEN・カバレッジ維持）
