# T1 完了条件チェック

タスク: フィールド型記法を日本語名称に統一
参照文書: 設計書 §5、スキーマ `$defs/field_def/type`、仕様リスト IV-12
変更対象: `src/test/resources/unit-test-yaml.xml`、`src/main/resources/nablarch/test/ntf-testdata-yaml-schema.json`、`src/test/.../YamlSchemaValidationTest.java`、`src/test/.../YamlTestDataParserTest.java`（または同等のファイル読込テスト）

## 実施手順（TDD）

1. RED: `dataTypeMapping` を登録しない設定で、`type: 半角`・`type: 全角`・`type: 数値` を持つ固定長/可変長 YAML を読み込み、`DataFileFragment` の型変換が例外なく成功することを検証するテストを追加し、失敗を確認する。
2. GREEN: `unit-test-yaml.xml` から `dataTypeMapping`（記号 identity mapping）の `<component>` 定義を削除する。
3. スキーマ `$defs/field_def/type` を、更新版スキーマ `03` の定義（`type: string`, `minLength: 1`, pattern なし）に置き換える。
4. `YamlSchemaValidationTest` に、`type: 半角` 等の日本語名称が valid であることを検証するケースを追加する。

## 完了条件チェックリスト

| 完了条件 | 担当者判定 | 担当者根拠 | QA判定 | QA根拠 |
|---|---|---|---|---|
| `dataTypeMapping` を登録しない設定で、日本語型の固定長/可変長 YAML が例外なく読み込める（RED→GREEN を確認） | OK / NG | | OK / NG | |
| `unit-test-yaml.xml` から identity mapping 定義が削除されている | OK / NG | | OK / NG | |
| スキーマで `type: 半角` 等の日本語名称が valid、かつ既存の日本語名称テストデータが全て valid | OK / NG | | OK / NG | |
| 既存テスト全グリーン（`Failures: 0, Errors: 0`） | OK / NG | | OK / NG | |

## QAエンジニアレビュー

| 観点 | 判定 | 根拠・改善案 |
|---|---|---|
| 目的に対して意味のあるテスト・動作確認が実施されているか | OK / NG | |
| エッジケースが漏れなくテスト・動作確認されているか | OK / NG | |

## エキスパートレビュー（ソースコード変更タスクのみ）

### 対象言語エキスパートレビュー

| 観点 | 判定 | 根拠・改善案 |
|---|---|---|
| ベストプラクティス準拠 | OK / NG | |
| 既存コードスタイル統一 | OK / NG | |
| テストコードのGWT形式 | OK / NG | |

### ソフトウエアエンジニアレビュー

| 観点 | 判定 | 根拠・改善案 |
|---|---|---|
| 責務分離の適切さ | OK / NG | |
| システム全体の整合性 | OK / NG | |
| 保守性・拡張性 | OK / NG | |

## 総合判定

- 担当者: OK / NG
- QA: OK / NG
- 対象言語エキスパート: OK / NG / 該当なし
- ソフトウエアエンジニア: OK / NG / 該当なし
- ユーザーレビュー可否: 可 / 不可（理由）
