# #21 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| YAML形式相当の構成（`sendSyncTestData` に拡張子未設定）で、配下ファイルを更新すると次回読み出しで1件目から応答電文が返る（再読み込みされる）ことが自動テストで示されている | OK | 再現テスト `src/test/java/nablarch/test/core/messaging/SendSyncSupportTest.java#testReloadTestDataInDirectory`（`:120`）。設定 `src/test/resources/nablarch/test/core/messaging/directory/directory-component-configuration.xml` は `sendSyncTestData` の `basePathSettings` のみを定義し `fileExtensions` を持たないため、`FilePathSetting#getFileIfExists` はディレクトリ `.../data/RM11AD0301` を返す。テストは (1) 1回目に `failureCode=test2` を取得 (2) 2回目・3回目に `no=[2]` `no=[3]` の `receive message did not exists.` を確認 (3) 配下の `RM11AD0301/message.txt` を内容だけ上書き（`Files.write` の TRUNCATE_EXISTING。ディレクトリエントリを変えないため親ディレクトリの mtime は不変）し `setLastModified(now+2000)`（戻り値を `assertTrue` で表明） (4) 4回目に `failureCode=test4`（更新後の1件目）が返ることを検証する。<br>サブディレクトリ配下の更新も `#testReloadWhenFileInSubDirectoryIsUpdated`（`:150`）で検証。`RM11AD0302/sub/nested.txt` の mtime だけを未来へ動かし、`no` が1へ戻る（再読み込みされる）ことを確認する。<br>**負のコントロール実測（2026-08-19）**: `getTimestampSignature` の再帰呼び出しを `child.lastModified()` に置き換えた1階層版で `mvn -o test -Dtest=SendSyncSupportTest` → `Tests run: 8, Failures: 1, Errors: 1`（`testGetTimestampSignatureChangesWhenFileInSubDirectoryIsUpdated:227` と `testReloadWhenFileInSubDirectoryIsUpdated:168`）。畳み込みを `Math.max` に戻した最大値版 → `Tests run: 8, Failures: 1`（`testGetTimestampSignatureDetectsUpdateUnderFutureTimestampedFile:288`。未来日時ファイルを含むディレクトリで他ファイルを更新しても最大値は張り付いたままで検知できないことを、同テスト内で `maxLastModified` を計算して対比表示している）。本実装ではいずれも `Tests run: 8, Failures: 0, Errors: 0, Skipped: 0`。 | OK | 負のコントロールを QA が独立に実測: `getTimestampSignature` を `return file.lastModified();`（修正前挙動）に戻すと `testReloadTestDataInDirectory:139`・`testReloadWhenFileInSubDirectoryIsUpdated:168` がいずれも `receive message did not exists` で失敗（Failures:2, Errors:2）。テストは実装に依存して落ちる＝空振りではない。Verification も同じ変異（M7）で同2件が落ちることを別 worktree で確認。<br>留保: 構成は実 YAML リーダではなく本 PR で追加した `TsvTestDataReader` による「拡張子なし＋リクエストID同名ディレクトリ」の再現。欠陥が依存するのは配置だけなので妥当だが、YAML そのものは通っていない。 |
| Excel形式（`sendSyncTestData` に拡張子設定あり）の既存挙動に変化がなく、既存テストが全件成功する | OK | Excel形式では `getFileIfExists` が返すのはファイルであり `File#isDirectory()` が false のため、`getTimestampSignature`（`SendSyncSupport.java:427-430`）は早期 return で `file.lastModified()` をそのまま返す。これを `#testGetTimestampSignatureReturnsLastModifiedWhenNotDirectory`（`SendSyncSupportTest.java:180`）で `assertThat(actual, is(file.lastModified()))` により完全一致で表明している。<br>Excel形式の再読み込みを検証する既存テスト `MockMessagingContextTest#test6` を含め `mvn -o clean jacoco:instrument test ... -Dtest='SendSyncSupportTest,MockMessagingContextTest'` → `Tests run: 19, Failures: 0, Errors: 0`。全件実行 `JAVA_HOME=/usr/lib/jvm/temurin-17-jdk-amd64 LANG=ja_JP.UTF-8 TZ=Asia/Tokyo mvn -o clean test` → `Tests run: 848, Failures: 0, Errors: 0, Skipped: 7` / `BUILD SUCCESS`（2026-08-19 実測。前ラウンドの 841 件に対し本ラウンドの追加テスト7件分が増加）。<br>テスト実行後に `target/test-classes` 配下の fixture が更新後内容・未来 mtime のまま残らないよう、`@After`（`SendSyncSupportTest.java:99`）で内容と mtime を復元している。実行後に src 側と `diff` 一致・target 側 mtime がコピー時刻であることを確認済み。 | OK | Excel 経路が実際に守られていることを QA が変異で確認: `getTimestampSignature` を `System.nanoTime()` に差し替えると `MockMessagingContextTest` 2件・`MockMessagingClientTest` 1件が失敗する（`src/test/resources/nablarch/test/core/messaging/web/file.xml:24` で `sendSyncTestData` → `xls`）。ラバースタンプではない。Verification も基準コミット `05eed6b` で `Tests run: 840, Failures: 0, Errors: 0, Skipped: 7`、`b22e5b9` で `848/0/0/7` を実測し、増分 `+8` が新設 `SendSyncSupportTest` の8メソッドと一致すること、Skipped 7件の内訳が基準と同一（`HtmlConvertTest` 2・`RequestTestingMessagingClientTest` 2・`DbAccessTestSupportTest` 2・`FileUtilsTest` 1。いずれも `java.specification.version` 等による既存の `Assume`）であることを確認。<br>ニュアンス: Excel 経路でも署名の**採取タイミング**が「読み込み後」→「読み込み前」に変わっている。安全側の変更であり、2026-08-19 のユーザー制約3そのもの。 |
| 修正が `SendSyncSupport.createTestDataInfo` 内で完結し、`FilePathSetting`（`nablarch-core`）に変更がない | OK | `src/main` 配下の変更は `src/main/java/nablarch/test/core/messaging/SendSyncSupport.java` の1ファイルのみ。変更箇所は `createTestDataInfo(DataType, String)`（署名を `getMessages` より前に1度だけ取得し `:360` で比較・記録の双方に使用）、`createTestDataInfo(DataType, String, String, String, long, String)`（`File` 引数を `long timestampSignature` に置き換え、ツリー走査を1回に削減）、package private の `getTimestampSignature(File)`（`:427`。`Arrays.sort(files)`（`:438`）でエントリ順を固定してから `signature * 31 + child` で畳み込む）、`TestDataInfo.timestampSignature`（`:490`。`private static class` のためAPI影響なし）で、いずれも `SendSyncSupport` 内に閉じている。可視性を private から package private へ広げたのは本タスクが新規に追加したメソッドのみで、既存の public/protected API は変更していない。`nablarch-core` は Maven 依存であり本リポジトリに `FilePathSetting` のソースは存在しない（変更なし）。<br>**署名の衝突により再読み込みが行われない可能性は確率的に残る**（畳み込みハッシュのため）。この旨は `getTimestampSignature` の Javadoc にも記載している。<br>カバレッジ（`mvn -o clean jacoco:instrument test jacoco:restore-instrumented-classes jacoco:report -Dtest='SendSyncSupportTest,MockMessagingContextTest'` → `mvn -o jacoco:report -Djacoco.dataFile=jacoco.exec`、`target/site/jacoco/jacoco.xml`）: `getTimestampSignature(File)` は LINE 11/11・BRANCH 6/6・INSTRUCTION 45/45 で全カバー（`listFiles()` が null を返す分岐を含む）。`createTestDataInfo` は2引数版が LINE 18/18・BRANCH 6/6、6引数版が LINE 4/4。`SendSyncSupport` クラス全体では LINE 153/163・BRANCH 32/36。未カバー行（146, 176-177, 184-185, 215-216, 223-224, 469）はいずれもログ出力・`can not happen` の既存経路で、本修正の対象外。 | OK | `git diff --stat 05eed6b..b22e5b9 -- src/main/` は `SendSyncSupport.java` の1ファイルのみ（+66/-15）。`FilePathSetting` は nablarch-core の依存 jar 側にあり本リポジトリにソースが存在しないため変更不可能。<br>3エキスパートとも「厳密には `createTestDataInfo` 内で完結しておらず、package private メソッド `getTimestampSignature` の新設と private ネストクラス `TestDataInfo` のフィールド改名を伴う」と指摘したが、いずれも非公開 API で外部影響がなく `SendSyncSupport` 内に閉じているため充足と判定する。 |

## QA Expert Review

（2ラウンド目。`b22e5b9` に対し隔離 worktree で再実行）

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | NG | 負のコントロール実測（`mvn -o test -Dtest=SendSyncSupportTest`、全8件）: 「畳み込み→最大値」で1件（`...DetectsUpdateUnderFutureTimestampedFile:288`）、「再帰→1階層」で2件（`...ChangesWhenFileInSubDirectoryIsUpdated:227`・`testReloadWhenFileInSubDirectoryIsUpdated:168`）、「全面 revert」で4件が落ちる。一方 **「`Arrays.sort` 削除」で0件、「署名採取を読み込み後に戻す」で0件**。<br>**QA-1（重）**: `Arrays.sort(files)` は一切テストされていない。`testGetTimestampSignatureIsStableForSameDirectory`（`:232-259`）は同一プロセスで未変更ディレクトリに3回呼ぶだけで、`listFiles()` は未変更ディレクトリなら決定的に同じ順序を返すためソート欠落を原理的に検出できない。しかも `:234` の Javadoc は証明していないことを断言している（steering 指摘H）。<br>**QA-2（中）**: ユーザー制約3への対応「署名を読み込み前に1度だけ採取」がカバレッジゼロ（steering 指摘J）。<br>**QA-3（中）**: 本ラウンドの主目的（最大値→畳み込み）がホワイトボックス1件でしか担保されず、E2E は素通りする（steering 指摘O）。<br>**QA-5（判断材料）**: 衝突は構成可能だが実務では踏まない。単一ファイルの変更は必ず検知される（係数 `31^k` は奇数で mod 2^64 可逆、`Δsig = Δmtime·31^k ≠ 0`。3位置×デルタ ±1・±31・961・29791・1000 で全て検知を実測）。衝突には2つの mtime が逆方向へ厳密に 31:1 の比で動く必要があり、編集・再生成・チェックアウトのいずれでも起きない。<br>**QA-6/7/8（欠陥なしと実測）**: symlink 循環で `StackOverflowError` は起きない／`SendSyncSupportTest` を失敗回を含め計8回連続実行後も `target/test-classes` は `src/test/resources` とバイト一致・mtime も初回コピー時のまま／`RM11AD0301`・`RM11AD0302` はリポジトリ内で本テスト専用（`df0ea24` で使っていた `RM11AD0201` は `RequestTestingMessagingClientTest.java:114` 他と衝突しており、`b22e5b9` の改番は実際に必要な修正だった）。 |

## Expert Reviews (axes the task needs)

### Craft Expert (coding)

（2ラウンド目。`b22e5b9` に対し隔離 worktree で再実行）

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | NG | **P-1（Medium。実測再現）**: 畳み込みに決定的な衝突クラスがある。`SendSyncSupport.java:441` はエントリ名も構造区切りも畳み込まないため、`RM11AD0302` のような「ファイル＋サブディレクトリ」構成では `sig = d*961 + 31*m(message.txt) + 31*s(sub) + n(nested.txt)` となり、`message.txt` と `sub` の `lastModified` が入れ替わると署名が完全一致する（`before = after = 1821518253916162` を実測）。コーディネーターも `:427-444` から算術的に確認済み。`:419` の「確率的に残る」は不正確（steering 指摘 I）。<br>**P-2（Low）**: `SendSyncSupport.java:52` の `/** Excel情報のキャッシュ */` が実態と不一致（steering 指摘 K）。`fileCache` が非 final・非同期・無制限・クリア手段なしである点は既存問題で本変更では悪化していない。<br>**P-3（Low・任意）**: 自己参照 symlink は Linux の ELOOP で `isDirectory()` が false になり `StackOverflowError` にならないことを実測。リンク追従の制御はないが欠陥ではない。<br>**P-5（OK）**: `getFileIfExists` の null チェック後に署名取得している／`listFiles()` の null を握り潰さずディレクトリ自身の日時にフォールバック（`:433-436`）／`Arrays.sort` は同一親配下の比較なので `File#compareTo` の OS 差は同一プロセス内で一貫し実害なし／Java 17 に対する API 適合も問題なし。 |
| Consistency with existing style | NG | **C-1（Medium）**: `SendSyncSupportTest.java:306` が `setReadable(false)` を `Assume` より前に `assertTrue` しており、既存の作法に反する。`src/test/java/nablarch/test/core/util/FileUtilsTest.java:505-509` の `assumeNotWindows()` が本リポジトリ唯一かつ確立した前例で、`:395-396` `:428-429` はいずれも権限操作の**前**に置いている（コーディネーターが実物で確認）。`PoiXlsReader.java:288` にも「Windows でテストできないので割愛」という同種の判断がある（steering 指摘 L）。<br>**C-2（Low）**: `:315` の `finally` 内 `assertTrue` が本来の失敗を隠す（steering 指摘 M）。<br>**C-3/C-4（Low）**: `src/main` を走査したところ package private メソッド自体は `ListMapParser`・`ServletForwardVerifier` 等に多数あるが「テストのため」と書いた前例はない。また `getTimestampSignature` の Javadoc は15行のメソッドに26行で、同ファイルの `createCacheKey`・`getMessages`（5〜7行）に比して過剰（steering 指摘 T）。<br>**C-5（OK）**: `Charset.forName("UTF-8")`（`src/main` に16箇所、`StandardCharsets` は0）、`org.junit.Assert.assertThat`（57ファイル）、ダイヤモンド演算子不使用、`@Rule SystemRepositoryResource`、`TemporaryFolder`、Given/When/Then コメント — いずれも既存流儀に合致。インデント・可視性・メソッド配置順も問題なし。<br>**Q-1〜Q-6（テストコード品質）**: `Arrays.sort` 削除で8件全通過（QA-1 と一致）／却下した最大値方式をテスト側に再実装している（`:326-336`、steering 指摘 N）／`:170` の期待値が `:159` と同値で再読み込みの有無を区別できない・`FUTURE_OFFSET_MILLIS` の用途不一致（steering 指摘 S）／`TsvTestDataReader` はリソースリークなし・引数検証メッセージは `PoiXlsReader.java:50` と逐語一致だが open 失敗時の例外型が `PoiXlsReader.java:191` と不一致（steering 指摘 T）。`Arrays.asList` の不変長は `TestDataParsingTemplate` が両経路で防御的コピーを取るため現状安全。 |

### Verification Expert (test)

（2ラウンド目。`b22e5b9` に対し隔離 worktree で再実行。総合は conditional pass）

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) | OK | `mvn -o clean test`: 基準 `05eed6b` で `Tests run: 840, Failures: 0, Errors: 0, Skipped: 7`、`b22e5b9` で `848/0/0/7`、ともに BUILD SUCCESS。増分 `+8` は新設 `SendSyncSupportTest` の8メソッドと一致。Skipped 7件は全て変更前から存在し本変更と無関係（内訳は QA 列に記載）。`SendSyncSupportTest` 自身は Skipped 0 で、`Assume` は本環境では発火せず分岐は実行された。<br>**べき等性**: `clean` なしで `-Dtest=SendSyncSupportTest` を3連続実行し毎回 8/0/0/0、`git status` は毎回空、`target/test-classes` 配下の mtime も3回とも同一値。<br>**例外時の後始末**: `setFutureLastModified` 直後に `fail()` を挿入して失敗させた後も `RM11AD0301/message.txt` は内容・mtime とも復元済み。<br>**順序依存**: `-Dtest=SendSyncSupportTest,MockMessagingContextTest` を正順・`reversealphabetical` 逆順（ログの `Running` 行で逆転を確認）で実行し、いずれも 19/0/0/0。`static fileCache` 起因の順序依存は観測されず。<br>**カバレッジ**: `createTestDataInfo(DataType,String)` LINE 18/18・BRANCH 6/6、6引数版 LINE 4/4、`getTimestampSignature(File)` LINE 11/11・BRANCH 6/6。missed 0。 |
| Coverage (edge cases / claims / steps) | NG | 変異テスト（M1〜M8）で以下が判明。**M3（`Arrays.sort` 削除）→ 落ちるテスト0件、全848が BUILD SUCCESS**。実測では同一ディレクトリの列挙順は `b,a,d,c,e,sub` で呼び出しをまたいで不変、かつソート順と異なる（steering 指摘 H）。**M8（署名採取を読み込み後に戻す）→ 0件**（steering 指摘 J）。**M4（非ディレクトリの早期 return 削除）→ 0件**。`File.listFiles()` は非ディレクトリに null を返すため早期 return は直後の `files == null` 分岐と意味論的に等価で、JaCoCo の分岐100%は「実行された」ことしか示していない。付随して `testGetTimestampSignatureReturnsLastModifiedWhenNotDirectory` は実装と同じ呼び出し（`file.lastModified()`）と比較しており、`setLastModified` した値そのものと比較していない（steering 指摘 Q）。M5（`long signature = lastModified` → `0L`）の killer は空ディレクトリテスト1件のみ、M6（null 分岐の戻り値変更）の killer は `testGetTimestampSignatureWhenFilesCanNotBeListed` 1件のみで、後者は `Assume` により root 実行 CI・Windows・WSL の DrvFs では無言でスキップされる（steering 指摘 R）。<br>**未カバー項目**: エントリの追加・削除（steering 指摘 P）／深さ3以上の多階層／署名衝突の特性化／`sendSync` 1回ごとにツリー全走査となる性能影響（従来は stat 1回）の計測。<br>**網羅できている項目（変異で killer を特定済み）**: `listFiles()` が null／配下0件／サブディレクトリ再帰／未来日時混在下の更新検知／Excel（非ディレクトリ）で `lastModified()` と一致。 |

## Overall Verdict

- Self-check: OK（本ラウンドで方式を署名方式へ変更し、レビュー指摘6件を処置。`mvn -o clean test` 全件 `Tests run: 848, Failures: 0, Errors: 0, Skipped: 7`）
- QA: NG（Completion criteria は3件とも OK。落とす理由は成果物ではなく検証の質 — QA-1/QA-2/QA-3）
- Design expert: N/A（既定の方針に沿った局所的な修正で、新たな構造・方式を作らないため未実施）
- Craft expert: NG（Medium-specific best practice に P-1、Consistency に C-1。いずれも実測付き）
- Verification expert: NG（Coverage。Artifact actually checked は OK。総合は conditional pass）
- Ready to check off: No — 指摘 H（`Arrays.sort` が原理的にテストで担保できず、Javadoc が担保していると誤記している）が畳み込みの設計そのものに関わり、2026-08-19 のユーザー制約2「`Arrays.sort(files)` は必須」の前提を覆すためユーザー判断待ち。残る指摘 I〜T は Valid としてトリアージ済みで、H の確定後に1回の修正ラウンドでまとめて対応する。修正ラウンドは 3 回上限のうち **2 回目**にあたる。

## 未カバーのまま残す判断

**`listFiles()` が null を返す分岐は本ラウンドでテスト済み**（`SendSyncSupportTest#testGetTimestampSignatureWhenFilesCanNotBeListed`、`:301`）。一時ディレクトリに `setReadable(false)` を行い `org.junit.Assume.assumeTrue` で `listFiles() == null` になる環境かをガードしてから検証する。実測環境では `Skipped: 0` で実際に到達し、`getTimestampSignature` は LINE 11/11・BRANCH 6/6。ただし **root 実行の Docker CI・Windows・WSL の DrvFs ではこの `Assume` が無言で発火し、当該分岐が未検証・未カバーになる**（Verification が M6 で killer を1件と確定）。この環境依存は残す。

**`Arrays.sort(files)` の走査順非依存は、テストで担保できない。** `listFiles()` は未変更ディレクトリに対し同一プロセス内で決定的に同じ順序を返すため、ソートを削除しても全848テストが通ることを3エキスパートが別々に実測した。列挙順を注入できる seam を設けない限り検証手段がない。指摘 H の決定次第で、この未カバー項目自体を消せる（選択肢 B・C）。

**署名の衝突は、確率的ではなく構造的なクラスとして存在する。** `signature * 31 + child` はエントリ名も構造区切りも畳み込まないため、ディレクトリ直下のファイルとサブディレクトリの係数がどちらも 31 になり、両者の `lastModified` が入れ替わると署名が一致する（Craft が実測再現）。ただし**単一ファイルの変更は必ず検知される**（係数 `31^k` は奇数で mod 2^64 可逆）。衝突には2つ以上の `lastModified` が逆方向へ厳密に 31:1 の比で動く必要があり、編集・再生成・チェックアウトのいずれでも起きない。指摘 H の決定次第で解消する（選択肢 A・B は衝突クラスを縮小、C は衝突そのものを消す）。

---

## 修正ラウンド2（指摘 H = 選択肢 C を採用。基準 `c5f3340`）

2026-08-20 ユーザー判断により、署名（`long` 畳み込み）を廃し `Map<相対パス, lastModified>` のスナップショットを `equals` 比較する方式（選択肢 C）へ変更した。

### 実装の変更点（`src/main` は `SendSyncSupport.java` の1ファイルのみ）

- `getTimestampSignature(File)`（`long`）を廃し、`getTimestampSnapshot(File)`（`Map<String, Long>`）と private な再帰ヘルパ `collectTimestamp(File, String, Map)` を新設。キーは起点からの相対パス（起点自身は空文字、区切りは環境非依存とするため `/` に固定）、値は `lastModified()`。ファイル・ディレクトリの双方をエントリとして含める
- `Arrays.sort(files)` を削除（Map の等価判定がキー順に依存しないため不要）。未使用となった `java.util.Arrays` の import も削除
- `TestDataInfo.timestampSignature`（`long`）→ `timestampSnapshot`（`Map<String, Long>`）。`private static class` のため公開 API に影響しない
- 比較を `!timestampSnapshot.equals(cachedTestDataInfo.timestampSnapshot)` に変更
- スナップショットの採取位置（`getMessages` の前に1度だけ、比較にも記録にも同じ値）は変更していない（2026-08-19 ユーザー制約3）
- Javadoc から「確率的に残る」を削除し、保持サイズと比較コストがエントリ数に比例すること（走査回数は不変であること）を明記。26行→11行に短縮（指摘 T）
- `fileCache` の Javadoc を `Excel情報のキャッシュ` → `読み込んだテストデータ情報のキャッシュ` に修正（指摘 K）

### 指摘 H〜T の処置

| # | 処置 | 根拠 |
|---|---|---|
| H | **解消（設計変更）** | `Arrays.sort` を削除。`grep -c "Arrays.sort" src/main/java/nablarch/test/core/messaging/SendSyncSupport.java` → `0`。走査順非依存は Map の等価判定の定義により構造的に保証され、テストで担保する必要自体が消えた |
| I | **修正不要（解消）** | 畳み込みを行わないため係数31の構造的衝突が原理的に存在しない。相対パスがキーであるため、直下ファイルとサブディレクトリの `lastModified` が入れ替わっても別キーの値として現れる |
| J | **修正した** | `SendSyncSupportTest#testReloadWhenTestDataIsUpdatedWhileReading` を追加。`TsvTestDataReader` を継承し `open()` で対象ファイルの `lastModified` を1度だけ未来へ動かすリーダ（`UpdatingWhileReadingTsvTestDataReader`）を `repositoryResource.addComponent` で差し込み、読み込み中の書き換えが次回の読み出しで検知されること、および読み込む番号が1へ戻ることを公開 API 経由で表明する。負のコントロールはゲート8を参照 |
| K | **修正した** | 上記のとおり `fileCache` の Javadoc を実態に合わせた |
| L | **修正した** | `testGetTimestampSnapshotWhenFilesCanNotBeListed` の冒頭に `assumeNotWindows()`（`FileUtilsTest.java:505-509` と同じ作法）を置き、`setReadable(false)` の結果は `assertTrue` ではなく `Assume.assumeTrue` で受けるよう改めた。権限操作の前に環境の前提を置く形になり、読み取り権限の概念がない環境では失敗でなくスキップとなる |
| M | **修正した** | 同テストの `finally` は復元結果を変数に受けるだけとし、`assertTrue` は try 文を抜けた後に置いた（本来の失敗を隠さない）。`restoreTestData`（`@After`）は各ファイルを try/catch で包んで全件の復元を試み、失敗したファイル名をまとめてから1度だけ表明する形に改めた |
| N | **修正した** | テスト側の `maxLastModified` ヘルパと、それを使う表明（旧 `:290`）を削除。`testGetTimestampSnapshotDetectsUpdateUnderFutureTimestampedFile` は production コードの戻り値の変化だけを表明する |
| O | **修正した** | `RM11AD0301/dummy.txt` を新設し、`testReloadWhenFutureTimestampedFileExists` を追加。ダミーファイルの `lastModified` を1日先に設定したうえで `message.txt` を更新し、`getResponseMessageByRequestId`（公開 API）経由で更新後の応答電文が返ることを表明する |
| P | **修正した** | `testGetTimestampSnapshotChangesWhenEntryIsAddedOrRemoved` を追加。エントリの追加でスナップショットが変化しキーが現れること、削除で再び変化しキー集合が元へ戻ることを表明する。C ではキー集合の変化として構造的に現れる |
| Q | **テストのみ修正（分岐は残す）** | `testGetTimestampSnapshotForNotDirectory` は、実装と同じ `file.lastModified()` 呼び出しではなく **`setLastModified` した値そのもの**（秒単位に切り捨てた値）と比較する形に改めた。メソッド名も `...Snapshot...` に改名。**なお、非ディレクトリの早期 return が冗長である事実は C を採っても変わらない**（下記「実測」参照）。可読性のための冗長分岐である旨をコードのコメントに明記した |
| R | **記録した** | `testGetTimestampSnapshotWhenFilesCanNotBeListed` は、Windows では `assumeNotWindows()` で、読み取り権限を落とせない環境では `Assume.assumeTrue(setReadable(false))` で、落としても一覧が取得できる環境（root 実行等）では `Assume.assumeTrue(listFiles() == null)` でスキップされる。**これらの環境では `files == null` 分岐が未検証・未カバーになる。** 本実行環境（WSL2 / ext4 / 非 root）では `Skipped: 0` で到達している |
| S | **修正した** | `testReloadWhenFileInSubDirectoryIsUpdated` の末尾に `assertResponseMessageNotExists(..., 2)` を追加し、再読み込みによって読み込む番号が1へ戻ったこと（番号の据え置きではないこと）を区別できるようにした。刻み幅の用途には `STEP_MILLIS` を新設し、`FUTURE_OFFSET_MILLIS`（未来方向のオフセット）との混用を解消した |
| T | **修正した** | `getTimestampSnapshot` の Javadoc を26行→11行に短縮（同ファイルの `createCacheKey`・`getMessages` は5〜7行）。`TsvTestDataReader#open` のオープン失敗時の例外を `IllegalArgumentException` → `RuntimeException("test data file open failed. ...")` に改め、`PoiXlsReader.java:191` と型・メッセージの流儀を揃えた |

### ゲートの実測（2026-08-20。すべて `JAVA_HOME=/usr/lib/jvm/temurin-17-jdk-amd64 LANG=ja_JP.UTF-8 TZ=Asia/Tokyo`）

| # | ゲート | 結果 | 実測 |
|---|---|---|---|
| 1 | 全件テストが `BUILD SUCCESS` かつ `Failures: 0, Errors: 0` | PASS | `mvn -o clean test` → `Tests run: 851, Failures: 0, Errors: 0, Skipped: 7` / `BUILD SUCCESS` |
| 2 | `Tests run` が 848 ＋ 追加分と一致 | PASS | 848 + 3 = 851。追加したのは `testReloadWhenFutureTimestampedFileExists`・`testReloadWhenTestDataIsUpdatedWhileReading`・`testGetTimestampSnapshotChangesWhenEntryIsAddedOrRemoved` の3件（`SendSyncSupportTest` 単体は 8→11 件） |
| 3 | `Skipped` の内訳が基準と同一 | PASS | `target/surefire-reports/*.xml` の `skipped` 属性を走査 → `DbAccessTestSupportTest` 2・`RequestTestingMessagingClientTest` 2・`FileUtilsTest` 1・`HtmlConvertTest` 2（計7）。基準と一致。`SendSyncSupportTest` は `Skipped: 0` |
| 4 | `src/main` の差分が `SendSyncSupport.java` の1ファイルのみ | PASS | `git diff --stat c5f3340..HEAD -- src/main/` → `SendSyncSupport.java` 1ファイル |
| 5 | `Arrays.sort` が0件 | PASS | `grep -c "Arrays.sort" src/main/java/nablarch/test/core/messaging/SendSyncSupport.java` → `0` |
| 6 | Javadoc に「確率的」が0件 | PASS | `grep -c "確率的" src/main/java/nablarch/test/core/messaging/SendSyncSupport.java` → `0` |
| 7 | 負のコントロール（`getTimestampSnapshot` を起点の `lastModified` 1件だけ返す実装＝修正前挙動に差し替え） | PASS | `mvn -o test -Dtest=SendSyncSupportTest` → `Tests run: 11, Failures: 3, Errors: 4`。落ちたのは `testGetTimestampSnapshotChangesWhenEntryIsAddedOrRemoved`・`testGetTimestampSnapshotChangesWhenFileInSubDirectoryIsUpdated`・`testGetTimestampSnapshotDetectsUpdateUnderFutureTimestampedFile`・`testReloadTestDataInDirectory`・`testReloadWhenFileInSubDirectoryIsUpdated`・`testReloadWhenFutureTimestampedFileExists`・`testReloadWhenTestDataIsUpdatedWhileReading` の7件。差し替えは実測後に戻した |
| 8 | 負のコントロール（スナップショットの採取を `getMessages` の後に移す） | PASS | 記録する値だけを読み込み後に採取し直す差し替えで `Tests run: 11, Failures: 0, Errors: 1`。落ちたのは `testReloadWhenTestDataIsUpdatedWhileReading` の1件（前ラウンドは0件だった）。差し替えは実測後に戻した |
| 9 | `steering.md` の Steps 更新と `checks/21.md` への記録 | PASS | 本節および `steering.md` の `#21` Steps |
| 10 | 公開 API（public / protected）の変更が0件 | PASS | `git diff -- src/main/ \| grep -E "^[+-]\s*(public\|protected)"` → 出力なし |

### 追加・改名・削除したテスト

- 追加（3件）: `testReloadWhenFutureTimestampedFileExists`（指摘 O）／`testReloadWhenTestDataIsUpdatedWhileReading`（指摘 J）／`testGetTimestampSnapshotChangesWhenEntryIsAddedOrRemoved`（指摘 P）
- 改名（5件。`...Signature...` → `...Snapshot...`）: `testGetTimestampSnapshotForNotDirectory`（旧 `testGetTimestampSignatureReturnsLastModifiedWhenNotDirectory`。内容も指摘 Q に従い改めた）／`testGetTimestampSnapshotForEmptyDirectory`／`testGetTimestampSnapshotChangesWhenFileInSubDirectoryIsUpdated`／`testGetTimestampSnapshotIsStableForSameDirectory`／`testGetTimestampSnapshotDetectsUpdateUnderFutureTimestampedFile`／`testGetTimestampSnapshotWhenFilesCanNotBeListed`
- 削除: テスト側の `maxLastModified` ヘルパと、それを使う表明（指摘 N）
- 追加したテストリソース: `RM11AD0301/dummy.txt`（指摘 O 用のダミーファイル）／`RM11AD0303/message.txt`（指摘 J 用。応答電文1件）

### C の採用で消えた未カバー項目・残った未カバー項目

**消えた項目**

- `Arrays.sort(files)` の走査順非依存（指摘 H）— コードごと消えたため、未カバー項目そのものが存在しない
- 署名衝突の特性化（指摘 I）— 畳み込みをしないため衝突が存在しない
- エントリの追加・削除（指摘 P）— `testGetTimestampSnapshotChangesWhenEntryIsAddedOrRemoved` でカバーした

**残った項目**

- **非ディレクトリの早期 return は、C を採っても冗長なままである（指摘 Q）。** `collectTimestamp` から `if (!file.isDirectory()) { return; }` を削除しても `SendSyncSupportTest` は `Tests run: 11, Failures: 0, Errors: 0` で全通過することを実測した（`File#listFiles()` が非ディレクトリに null を返すため、直後の `files == null` 分岐で同じ結果になる）。「C を採れば等価な冗長分岐でなくなる」という見込みは実測では成立しなかった。指示どおり分岐は残し、可読性のための冗長分岐である旨をコードのコメントに明記している
- `files == null` 分岐は、Windows・読み取り権限を落とせない環境・root 実行環境ではスキップされ未カバーになる（指摘 R）
- 深さ3以上の多階層のツリー
- `sendSync` 1回ごとにツリーを全走査する性能影響の計測（走査回数は署名方式と同じで、本ラウンドで悪化していない。保持サイズと比較コストはエントリ数に比例する）

### 本ラウンド未実施

- QA・Craft・Verification の3レビュー再実行（`steering.md` `#21` Steps の該当項目）は未実施。本ラウンドの作業指示にゲートとして含まれていないため、次のステップとして残している
