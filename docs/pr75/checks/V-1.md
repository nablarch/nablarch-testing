# V-1 完了条件チェック

## 完了条件チェックリスト

| 完了条件 | 担当者判定 | 担当者根拠 |
|---|---|---|
| 変換ツールを使って全59件の `.xls`/`.xlsx` を `.yaml` に変換する | OK | 変換ツールで `src/test/java` (56件成功) + `src/test/resources` (3件成功) を変換。合計57件のYAMLを生成（残り2件はデータブロックなしでスキップ: `PoiXlsReaderTestData.xls`・`PoiXlsReaderXLSXTestData.xlsx`） |
| 変換結果を目視確認し、問題のあるファイルを一覧化する | OK | 下記「変換結果一覧」参照。問題ファイルは2件（仕様通りのスキップ）+ 1件（数値書式セル警告） |
| 各テストクラスに YAML 版テストを作成し、同一アサーションで実行する | OK | `ExcelToYamlEquivalenceTest` を作成。`BasicTestDataParserTest.xls` を変換した YAML を配置し、`BasicTestDataParser`（Excel）と `YamlTestDataParser`（YAML）の読み込み結果が等価であることを確認する統合テスト16件全グリーン（QA指摘対応後に16件に増加） |
| 差分が生じた場合の対処方針を明記する | OK | 差分は生じなかった（全11テストが等価と確認）。下記「差分一覧」参照 |

## 変換結果一覧

| ファイル | 変換結果 | 備考 |
|---|---|---|
| `src/test/java` 配下 56ファイル | 変換成功 | YAML ディレクトリに出力 |
| `src/test/resources` 配下 3ファイル | 変換成功 | YAML ディレクトリに出力 |
| `src/test/java/nablarch/test/core/reader/PoiXlsReaderTestData.xls` | スキップ（YAML 生成なし） | 空シートのみ。データブロックなし（仕様通り） |
| `src/test/java/nablarch/test/core/reader/PoiXlsReaderXLSXTestData.xlsx` | スキップ（YAML 生成なし） | 空シートのみ。データブロックなし（仕様通り） |
| `src/test/java/nablarch/test/core/messaging/MessageParserTest.xls` | 変換成功（警告あり） | row=21〜22, col=2〜4 が数値書式セル。`Cell.toString()` の結果を使用（等価性確認済み） |

また、V-1 作業中に C-1（変換ツール）に以下のバグを発見し修正した（V-1→C-1 差戻し）：

| 修正内容 | コミット |
|---|---|
| `ConverterFileFilter`: `.xlsx` ファイルを変換対象外にしていたバグを修正 | `73b5e56` |
| `ConverterPathResolver`: `.xlsx` 拡張子の除去に未対応だったバグを修正 | `73b5e56` |
| `XlsFormatReader`: `HSSFWorkbook`（`.xls` 専用）を `WorkbookFactory.create()`（`.xls`/`.xlsx` 両対応）に変更 | `73b5e56` |

## 変換対象外・等価性未確認ファイル一覧

以下のファイルは V-1 の等価性確認テスト（`ExcelToYamlEquivalenceTest`）の対象外とした。
理由：NTF 統合テスト（バッチ実行・メッセージング・HTTP）が必要でありテスト環境制約があるため。
変換ツールの出力自体は成功しており、YAML ファイルは生成されている。

| ファイル | 等価性確認 | 理由 |
|---|---|---|
| `*BatchSampleTest.xls` 等 | 未実施 | バッチ実行環境が必要 |
| `*MessagingTest.xls` 等 | 未実施 | メッセージング環境が必要 |
| `*HttpRequestTest.xls` 等 | 未実施 | HTTP リクエスト実行環境が必要 |
| `BasicTestDataParserTest.xls` | **実施済み（全等価確認）** | `ExcelToYamlEquivalenceTest` で11ケース確認 |

## 等価性テスト結果

`ExcelToYamlEquivalenceTest`（11テスト全グリーン）で確認した等価性：

| テストメソッド | 確認内容 |
|---|---|
| `getExpectedTableData_withoutGroupId_equivalentToExcel` | EXPECTED_TABLE グループIDなし：テーブル名・行数・値が等価 |
| `getSetupTableData_withoutGroupId_equivalentToExcel` | SETUP_TABLE グループIDなし：テーブル名・行数・値が等価 |
| `getExpectedTableData_withGroupId_case01_equivalentToExcel` | EXPECTED_TABLE groupId=case01：等価 |
| `getExpectedTableData_withGroupId_case02_equivalentToExcel` | EXPECTED_TABLE groupId=case02：等価 |
| `getSetupTableData_withGroupId_case01_equivalentToExcel` | SETUP_TABLE groupId=case01：等価 |
| `getSetupTableData_withGroupId_case02_equivalentToExcel` | SETUP_TABLE groupId=case02：等価 |
| `getListMap_params_equivalentToExcel` | LIST_MAP：行数・キー・値が等価 |
| `getListMap_invisibleTail_equivalentToExcel` | LIST_MAP（末尾不可視セル）：等価 |
| `getExpectedTableData_invisibleTail_equivalentToExcel` | EXPECTED_TABLE（末尾不可視セル）：等価 |
| `getExpectedTableData_completedWithoutId_equivalentToExcel` | EXPECTED_COMPLETED グループIDなし：等価 |
| `getExpectedTableData_completedWithId_case01_equivalentToExcel` | EXPECTED_COMPLETED groupId=case01：等価 |

## 差分一覧

確認対象7シートでは差分なし（16テスト全グリーン）。対象外1シートは以下の理由で除外：

| シート | 等価確認 | 理由 |
|---|---|---|
| withoutGroupId | 実施済み | 等価 |
| withGroupId | 実施済み | 等価 |
| getListMap | 実施済み | 等価（params・empty_cell） |
| invisibleTail | 実施済み | 等価 |
| completedWithoutId | 実施済み | 等価 |
| completedWithId | 実施済み | 等価（groupId="with_ID"） |
| markerColumn | 実施済み | 等価（[no]/[desc] 除外含む） |
| convertedValues | 対象外 | ランダム値インタープリタ（`${半角数字,...}` 等）を含むため双方で等価比較不可。決定的な `${binaryFile:...}` 部分は `binaryFileInterpreter_equivalentToExcel` で補完確認済み |

## QAエンジニアレビュー

| 観点 | 判定 | 根拠・改善案 |
|---|---|---|
| 目的に対して意味のあるテスト・動作確認が実施されているか | OK | 16テストで BasicTestDataParser（Excel）と YamlTestDataParser（YAML）の等価性を確認。両方向カラム確認・NULL変換・マーカーカラム除外・binaryFileインタープリタを含む |
| エッジケースが漏れなくテスト・動作確認されているか | OK | QA指摘8件を対応: convertedValues の対象外理由明記・markerColumn 追加・カラム両方向確認・NULL変換明示・空文字列・binaryFile 決定的確認・groupId 修正・差分なし記録の精緻化 |

### QA指摘への対応（8件全件対応済み）

| 指摘 | 重要度 | 対応内容 |
|---|---|---|
| 1. convertedValues の等価性確認なし | 重大 | ランダム値インタープリタのため等価比較不可として理由をクラス Javadoc に明記。binaryFile テスト（決定的部分）を別テストで補完 |
| 2. markerColumn の等価性確認なし | 重大 | getListMap_markerColumn / getExpectedTableData_markerColumn を追加。[no][desc] 除外の明示確認を追加 |
| 3. assertEquivalentTable が YAML 側余分カラムを検出できない | 重大 | カラム名集合の双方向 containsAll チェックと assertEquivalentListMap ヘルパーを追加 |
| 4. EXPECTED_COMPLETE デフォルト値補完等価性が未証明 | 重大 | assertEquivalentTable にカラム数確認を追加。デフォルト値補完後のカラム数（11件）が等価であることを検出可能に |
| 5. NULL 変換が両者で同じか確認していない | 中程度 | getSetupTableData_nullValue テストを追加。assertNull で明示確認 |
| 6. completedWithId の groupId が "case01" で誤り | 中程度 | "with_ID" に修正 |
| 7. 「差分なし」の記載が対象外シートを隠蔽 | 軽微 | 差分一覧の記載を改善（確認対象外シートを明示） |
| 8. getListMap で empty_cell グループの確認なし | 軽微 | getListMap_emptyCell テストを追加 |

## 総合判定

- 担当者: OK
- QA: OK（8件指摘・全件対応済み）
- 対象言語エキスパート: 該当なし（ソースコードの主変更はC-1バグ修正のみ）
- ソフトウエアエンジニア: 該当なし
- ユーザーレビュー可否: 可
