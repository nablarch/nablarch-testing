# P3-5 Completion Check — #5 TestDataParserAdapter 新設（判断 A）

対象成果物:
- `src/main/java/nablarch/test/core/reader/TestDataParserAdapter.java`（新規・本体パッケージ相乗り 1 枚）
- `src/test/java/nablarch/test/core/reader/TestDataParserAdapterTest.java`（新規・14 件）

## Completion Criteria

| Criterion | Self-check | Evidence |
|---|---|---|
| 各メソッドが本体器（DataFile/TableData/MessagePool/List<Map>）を返す | OK | `readTables`→`List<TableData>`、`readListMap`→`List<Map<String,String>>`、`readFiles`→`List<? extends DataFile>`（Fixed/Variable）、`readMessage`→`MessagePool`。各テストで戻り値型・中身を検証（`readTablesReturnsRawTableData` ほか 14 件） |
| IN 値が記法のまま（未加工）であることをテストで実証 | OK | 空 interpreters 配線により `${...}`/空文字/null が未加工で取り出される。テーブル: `getValue` が `${userName}`/``/`null` を保持（`readTablesReturnsRawTableData`）。list_map: `${id}` 保持（`readListMapReturnsRawRows`）。ファイル: `${value}`/`${v}`/`${e}`/`${s}` 保持。メッセージ: FW ヘッダ `${rid}` 保持（`readMessageReturnsRawMessagePool`）。後処理なしも実証（`readTablesDoesNotFillDefaultValues`＝EXPECTED_COMPLETED で補完されない） |
| 相乗りがこのアダプタ 1 クラスに限局 | OK | package-private な `getResult`／`TableDataParser`・`ListMapParser` コンストラクタへのアクセスは `nablarch.test.core.reader.TestDataParserAdapter` の 1 クラスのみ。本体への他の変更なし（`git diff --stat` で main 追加は本クラス 1 ファイルのみ） |

## カバレッジ（JaCoCo 実測）

- `TestDataParserAdapter`（外側）: **命令 121/121＝100%・分岐 5/5＝100%（C0/C1 100%）**
- `TestDataParserAdapter$StubDbInfo`: `getColumnType` のみ被覆（読み込み経路で唯一呼ばれる）。他 9 メソッドは DB 書き込み経路専用で読み込み経路から呼ばれない**番人コード**（呼ばれたら `UnsupportedOperationException` で即失敗）＝steering ルール「C0/C1 100%（番人コード除く）」の除外対象

取得手順: `mvn package -Dmaven.javadoc.skip=true -Dtest=TestDataParserAdapterTest` → `mvn jacoco:report -Djacoco.dataFile=$(pwd)/jacoco.exec`

## テスト結果

- `TestDataParserAdapterTest`: 14 件 GREEN（Failures:0 Errors:0）
- reader パッケージ単体テスト回帰なし: `FixedLengthFileParserTest`/`VariableLengthFileParserTest`/`HeaderLineTest`/`TestDataParsingTemplateTest`/`SingleDataParsingTemplateTest`/`DataTypeTest`＋本テスト＝32 件 GREEN

## スコープ判断（記録）

- expected_request_*/response_* 系メッセージ（`SendSyncMessageParser`/`GroupMessageParser`・`getMessageWithoutCache`/`getSendSyncMessage` 相当）は **#5 では非搭載**。設計書クラス図のアダプタ公開面は `readFiles/readTables/readListMap/readMessage` の 4 メソッドであり、#5 完了条件の本体器も 4 種（DataFile/TableData/MessagePool/List<Map>）。当該メッセージ種別は XlsFormatReader が全種別を中間モデル化する **#6 で必要に応じ追加**（grounding の申し送り通り）。中間モデル `MessageDataBlock` は 5 種別対応済のため #6 で接続可能。

## 設計整合（判断 A）

- 空 interpreters（`Collections.emptyList()`・`BinaryFileInterpreter` を積まない）で配線し `parse → getResult`。`InterpretationContext.invokeNext()` が interpreters 空で値素通し（確認済）
- 後処理（`fillDefaultValues`・EXPECTED マージ）を持ち込まない（`getExpectedTableData` の挙動は再現しない）
- カラム名大文字化・型シンボル変換は本体器（`TableData`/`DataFileFragment`）固有の構造挙動で判断 A では受容（IN「値」の未加工性とは別軸。D-F 参照）

## 4 観点サブエージェントレビュー

独立サブエージェント（Agent ツール・会話履歴なし）で QA／Java 言語エキスパート／ソフトウェアエンジニアの 3 観点を実施（アーキテクト観点は判断 A 整合として SWE/設計整合に統合）。

### QA エンジニア — イテレーション 2 で PASS
- **イテレーション 1（FAIL）**: 旧 `readTablesDoesNotFillDefaultValues` が偽 GREEN との指摘。`TableData.fillDefaultValues()` は「省略カラム（test データに無い DB カラム）」のみ補完し宣言済み空セルは触れないため、宣言済みカラムの空文字 assert は本体正規経路でも同結果＝識別力ゼロ。
- **対応**: テストを `readTablesDoesNotPostProcessExpectedCompleted` に作り替え。補完が走れば `StubDbInfo.getColumns`（番人＝`UnsupportedOperationException`）が呼ばれて落ちる設計を逆手に取り、「例外なく完了」＋「カラム数が宣言数のまま（DB 全カラムへ拡張されない）」で後処理なしを**識別的に**実証。加えて `readTablesDoesNotMergeExpectedTypes`（EXPECTED_TABLE と EXPECTED_COMPLETE_TABLE 併置で readTables(EXPECTED_COMPLETED) が COMP 単独＝マージしない）を新設。
- **イテレーション 2（PASS）**: 二値判定（例外送出）＋カラム数拡張の二重防御で識別力ありと確認。軽微指摘3（マーカーカラムのファイル/メッセージ系未検証）は **非該当**（marker 除外は `HeaderLine` 経由＝TABLE/LIST_MAP のみの概念。File/Message は HeaderLine 不使用と grep 確認）。軽微指摘4（コンストラクタ null チェック）は **追加不要**（既存 `BasicTestDataParser` 等もハウススタイルとして null チェックを置かない）。

### Java 言語エキスパート — PASS
- final/不変性・アクセス修飾子最小化・switch（許容タイプ列挙＋default で IAE）・例外設計（不正タイプ=IAE／番人=UOE）・Javadoc 日本語スタイル・`@Override`・GWT テスト・`FakeTestDataReader` 設計いずれも OK。
- ワイルドカード戻り型 `List<? extends DataFile>` は Fixed/Variable を 1 メソッドで束ねる設計上妥当・型安全。
- 番人設計を読み込み経路（`addRow`→`getColumnTypes`＝`getColumnType` のみ／`getColumnNames` は columnNames 明示設定済で `getColumns` 非到達）でコード確認し妥当と確認。
- 非ブロッカー軽微: `java.sql.Types.VARCHAR` の FQCN 直書き・`@version` 欠落（PASS を妨げない）。

### ソフトウェアエンジニア — PASS
- 責務分離（配線＋取り出しに徹し値加工・後処理・中間モデル組み立てを持ち込まない）OK。システム整合性（本体 4 Parser 無変更を `git diff main` で確認・相乗り 1 クラス限局・package-private 壁突破の局所化）OK。保守性（重複なし・マジックナンバーなし・switch 分岐網羅）OK。判断 A 整合（空 interpreters・`BinaryFileInterpreter` 非積載・生器返却・後処理なし・MESSAGE 経路）OK。
- expected_request_*/response_* 系を #6 送りにしたスコープ判断は設計書のアダプタ公開面（4 メソッド）と整合し妥当と確認。

## Overall Verdict

- Self-check: OK
- QA: **PASS**（イテレーション 2）
- Language expert: **PASS**
- Software engineer: **PASS**
- Ready for user review: Yes（Operating mode によりユーザーレビューはスキップ可）
